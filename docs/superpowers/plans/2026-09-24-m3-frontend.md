# M3 前端（issue #26）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 观众打开链接实时观看全 AI 对局（默认上帝视角）并在终局后拖动回放；前端用与后端逐事件等价的 TS `reduce()`；用户在 UI 里配置模型服务（Provider）、定义 Agent 档案并分配座位开局。

**Architecture:** 后端补 4 块（GM token；Agent 档案库 + `/skills` `/presets`；Provider 存储 + 凭据按端口注入 LiteLLM 客户端；金样导出 CLI + 静态挂载）。前端 `frontend/`（Vite + React 18 + TS strict + Zustand + Vitest）：`src/engine/` 纯函数（types / reduce / normalize / phases / select，金样对拍）；`src/api/`（REST、WS hook、agents、providers）；`src/store/`（事件日志 + 增量 reduce + 回放游标检查点）；`src/components/` 与 `src/pages/` 按 Claude Design 定稿实现（`docs/design/m3-ui-mockups.html`，Nocturne tokens `docs/design/nocturne.css`）。前端零信息过滤。

**Tech Stack:** Python 3.11 / Pydantic v2 / FastAPI / LiteLLM / httpx（后端，uv、ruff 100、mypy strict）；Node 20 / Vite 5 / React 18 / TypeScript 5 strict / Zustand 4 / Vitest / @testing-library/react / ESLint + Prettier / CSS Modules（前端，npm）。

**Spec:** `docs/superpowers/specs/2026-09-23-m3-frontend-design.md`（§7.5 设计定稿为 UI 依据）

## Global Constraints

- 引擎（`app/engine/`）零改动；前端不做任何可见性过滤，所有裁剪由服务端 token 决定。
- `app/runtime`、`app/api`、`app/cli` 模块级不 import `app.agent.agent_player` / `app.agent.llm_client`（`tests/test_agent_profile.py::test_importing_registry_does_not_load_litellm` 守卫）；`litellm` / `httpx` 只在函数内 import 或注入。
- 密钥永不回传、永不写入 `GameMeta` / 事件日志 / 建局回显 / 日志；API 响应只给 `has_key` + 末 4 位。
- `provider` 为空的档案行为与现状逐字相同（`model` 含 LiteLLM 前缀、凭据走环境变量）。
- TS `reduce` 语义逐条对齐 `backend/app/engine/events.py::reduce`；集合字段在 TS 为升序 `number[]`；未知事件类型抛错；每事件 `state_version + 1`。
- 金样：`states[i]` = `events[:i+1]` 之后的状态；`frozenset` 字段导出时升序。
- 文档与代码注释中文；标识符英文；后端 ruff 行宽 100（中文宽 2）；前端 Prettier 默认（printWidth 100）。测试零 IO 零 mock（后端；文件用 `tmp_path`；Provider 探测用注入的假 probe 对象）；前端允许假 `fetch` / 假 `WebSocket` / 假 probe。
- 每任务结束前后端全绿（`backend/`）：`uv run pytest -q -x --ignore=tests/test_api_e2e.py`、`uv run ruff check .`、`uv run ruff format --check .`、`uv run mypy app`；前端任务：`cd frontend && npm run check`。
- 设计定稿是 UI 的真值：组件的结构、尺寸、颜色、文案取自 `docs/design/m3-ui-mockups.html` 对应 `id`（1a–1j、2a–2d、3a–3d）的 HTML；tokens 取自 `docs/design/nocturne.css`。

---

### Task 1: 后端 GM token 与 `/state` GM 分支

**Files:**
- Modify: `backend/app/api/deps.py`、`backend/app/schemas/games.py`、`backend/app/api/rest.py`、`backend/app/api/ws.py`
- Test: `backend/tests/test_api_lobby.py`、`backend/tests/test_api_ws.py`（追加）

**Interfaces:**
- Produces: `TokenInfo.kind` 含 `"GM"`；`CreateGameResponse.gm_token: str`；GM 可读 `/state`（`GameState` 全量）/`/events`/`/speeches`/`/replay`/`/meta`/WS，viewer `"GM"`。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/test_api_lobby.py` 末尾：

```python
def test_gm_token_reads_full_state_events_and_cannot_act_or_start() -> None:
    """issue #26：GM token 拿到全量（含 GM_ONLY / WOLVES / ROLE_SELF 与 meta.skills）；不能行动 / 开局。"""
    import time as _t

    from app.runtime.player_port import BotPlayerPort

    class _Skilled(BotPlayerPort):
        last_skills_used = ("wolf-claim-jump",)

    app = create_app(
        store=InMemoryEventStore(),
        timeouts=RunnerTimeouts(speech_sec=5.0, action_sec=5.0),
        agent_port_factory=lambda seat, h: _Skilled(state_provider=h.live_state),
    )
    client = TestClient(app)
    body = {"preset": "std_9_kill_side", "config_override": {"seed": 5}, "agents": {"*": {"model": "m"}}}
    created = client.post("/api/v1/games", json=body).json()
    gid, host, gm, spec = created["game_id"], created["host_token"], created["gm_token"], created["spectator_token"]
    assert gm and gm != host and gm != spec
    assert client.post(f"/api/v1/games/{gid}/start", json={}, headers=_auth(gm)).status_code == 403
    assert client.post(f"/api/v1/games/{gid}/start", json={}, headers=_auth(host)).status_code == 200
    handle = client.app.state.games.get(gid)  # type: ignore[attr-defined]
    deadline = _t.time() + 30
    while not (handle.task is not None and handle.task.done()) and _t.time() < deadline:
        _t.sleep(0.05)
    state = client.get(f"/api/v1/games/{gid}/state", headers=_auth(gm)).json()
    assert state["phase"] == "GAME_OVER" and {p["role"] for p in state["players"]} >= {"WEREWOLF", "SEER"}
    assert "state_version" in state and "sheriff_confirmed" in state
    events = client.get(f"/api/v1/games/{gid}/events", headers=_auth(gm)).json()
    vis = {e["visibility"] for e in events}
    assert {"PUBLIC", "GM_ONLY", "WOLVES", "ROLE_SELF"} <= vis
    assert any(e["meta"].get("skills") == "wolf-claim-jump" for e in events)
    spec_events = client.get(f"/api/v1/games/{gid}/events", headers=_auth(spec)).json()
    assert all(e["visibility"] == "PUBLIC" and "skills" not in e["meta"] for e in spec_events)
    assert client.get(f"/api/v1/games/{gid}/speeches", headers=_auth(gm)).status_code == 200
    assert client.get(f"/api/v1/games/{gid}/replay", headers=_auth(gm)).status_code == 200
    assert client.get(f"/api/v1/games/{gid}/meta", headers=_auth(gm)).status_code == 200
    r = client.post(f"/api/v1/games/{gid}/actions", json={"tool": "vote", "arguments": {}}, headers=_auth(gm))
    assert r.status_code == 403
    assert client.get(f"/api/v1/games/{gid}/my-turn", headers=_auth(gm)).status_code == 403
```

追加到 `backend/tests/test_api_ws.py` 末尾（沿用该文件的 `_auth` / `_start_ai_game` 或建局辅助；若无 `_start_ai_game`，按 `test_api_lobby` 的建局 + start 写法）：

```python
def test_ws_gm_stream_is_unfiltered(client: TestClient) -> None:
    created = client.post(
        "/api/v1/games", json={"preset": "std_9_kill_side", "config_override": {"seed": 7}}
    ).json()
    gid, host, gm = created["game_id"], created["host_token"], created["gm_token"]
    assert client.post(f"/api/v1/games/{gid}/start", json={}, headers=_auth(host)).status_code == 200
    vis: set[str] = set()
    with client.websocket_connect(f"/api/v1/ws?token={gm}") as ws:
        while True:
            frame = ws.receive_json()
            if frame["type"] == "game_event":
                vis.add(frame["event"]["visibility"])
            if frame["type"] == "game_over":
                break
    assert {"PUBLIC", "GM_ONLY"} <= vis
```

- [ ] **Step 2: 跑测试确认失败**

Run（`backend/`）：`uv run pytest tests/test_api_lobby.py tests/test_api_ws.py -q -k gm` → `KeyError: 'gm_token'`。

- [ ] **Step 3: 实现**

`app/api/deps.py`：`kind: Literal["HOST", "PLAYER", "SPECTATOR", "GM"]`（注释：`GM` = 上帝视角只读全量，issue #26）。

`app/schemas/games.py` `CreateGameResponse`：在 `spectator_token` 之后加 `gm_token: str  # 上帝视角只读 token（issue #26）；只发给建局者`。

`app/api/rest.py`：
- 建局：`gm_token = tokens.issue(TokenInfo(game_id=handle.game_id, seat=None, kind="GM"))`，响应传 `gm_token=gm_token`。
- 新增助手（放 `_handle_for` 附近）：
  ```python
  def _viewer_for(info: TokenInfo) -> Any:
      """token → 可见性 viewer：座位号 / "SPECTATOR" / "GM"（GM 全量，issue #26）。"""
      if info.kind == "PLAYER":
          return info.seat
      return "GM" if info.kind == "GM" else "SPECTATOR"
  ```
- `state_endpoint`：`require_kind(info, game_id, "PLAYER", "SPECTATOR", "GM")`；在 PLAYER 分支之后加 `if info.kind == "GM": return live.model_dump(mode="json")`。
- `speeches_endpoint`、`events_endpoint`：`require_kind(..., "PLAYER", "SPECTATOR", "GM")`；`viewer = _viewer_for(info)`。`replay_endpoint`、`meta_endpoint`：kinds 加 `"GM"`。`actions` / `my-turn` 不变（PLAYER only）。

`app/api/ws.py`：`if info.kind not in ("PLAYER", "SPECTATOR", "GM")` → 4403；`viewer: Any = info.seat if info.kind == "PLAYER" else ("GM" if info.kind == "GM" else "SPECTATOR")`。

- [ ] **Step 4: 跑测试确认通过 + 全量**

Run: `uv run pytest tests/test_api_lobby.py tests/test_api_ws.py tests/test_api_e2e.py -q` → PASS；四条命令全绿。`test_create_legacy_ai_model_echoes_star` 若精确比对整个响应体需补 `gm_token`（用 `r.json()["gm_token"]` 动态取值或改为逐字段断言）。

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/deps.py backend/app/schemas/games.py backend/app/api/rest.py backend/app/api/ws.py backend/tests/test_api_lobby.py backend/tests/test_api_ws.py
git commit -m "feat(api): GM token——上帝视角只读全量（/state 全量 GameState、事件流不过滤、WS） (issue #26)"
```

---

### Task 2: 后端 Agent 档案库 + `/skills` + `/presets`

**Files:**
- Create: `backend/app/runtime/agent_library.py`、`backend/app/api/agents.py`、`backend/app/schemas/presets.py`
- Modify: `backend/app/main.py`
- Test: `backend/tests/test_agent_library.py`、`backend/tests/test_api_agents.py`（新）

**Interfaces:**
- Produces: `StoredAgent{agent_id, profile: AgentProfile, created_at, updated_at}`；`AgentLibraryStore` 协议 `list()/get(id)/put(stored)/delete(id)`；`InMemoryAgentLibrary`、`JsonFileAgentLibrary(dir)`；`create_app(agents_dir=None, agent_library=None)`；端点 `GET/POST /api/v1/agents`、`GET/PUT/DELETE /api/v1/agents/{id}`、`GET /api/v1/skills`、`GET /api/v1/presets`。

- [ ] **Step 1: 写失败测试**

`backend/tests/test_agent_library.py`：

```python
"""Agent 档案库存储（issue #26）：round-trip、原子写、坏文件跳过、目录惰性。"""

import json

from app.agent.profile import AgentProfile
from app.runtime.agent_library import InMemoryAgentLibrary, JsonFileAgentLibrary, StoredAgent


def _stored(agent_id: str = "a_00000001", name: str = "夜枭") -> StoredAgent:
    return StoredAgent(
        agent_id=agent_id,
        profile=AgentProfile(name=name, model="ollama/qwen2.5:14b", skills=("logic-chain",)),
        created_at="2026-09-24T00:00:00+00:00",
        updated_at="2026-09-24T00:00:00+00:00",
    )


def test_in_memory_crud() -> None:
    lib = InMemoryAgentLibrary()
    assert lib.list() == [] and lib.get("a_x") is None
    lib.put(_stored())
    assert lib.get("a_00000001").profile.name == "夜枭"  # type: ignore[union-attr]
    lib.put(_stored(name="铁齿"))
    assert [s.profile.name for s in lib.list()] == ["铁齿"]
    assert lib.delete("a_00000001") is True and lib.delete("a_00000001") is False


def test_json_file_roundtrip_lazy_dir_and_corrupt_skipped(tmp_path, caplog) -> None:
    d = tmp_path / "agents"
    lib = JsonFileAgentLibrary(d)
    assert lib.list() == [] and not d.exists()
    lib.put(_stored())
    assert (d / "a_00000001.json").exists() and sorted(p.name for p in d.iterdir()) == ["a_00000001.json"]
    got = JsonFileAgentLibrary(d).get("a_00000001")
    assert got is not None and got.profile.skills == ("logic-chain",)
    (d / "a_bad.json").write_text("{bad", encoding="utf-8")
    assert [s.agent_id for s in JsonFileAgentLibrary(d).list()] == ["a_00000001"]
    assert "a_bad.json" in caplog.text
    assert lib.delete("a_00000001") is True and not (d / "a_00000001.json").exists()
```

`backend/tests/test_api_agents.py`：

```python
"""Agent 档案库 API + /skills + /presets（issue #26）。"""

import pytest
from fastapi.testclient import TestClient

from app.main import create_app
from app.runtime.agent_library import InMemoryAgentLibrary
from app.store.event_store import InMemoryEventStore


@pytest.fixture()
def client() -> TestClient:
    return TestClient(create_app(store=InMemoryEventStore(), agent_library=InMemoryAgentLibrary()))


def test_agents_crud_and_uniqueness(client: TestClient) -> None:
    r = client.post("/api/v1/agents", json={"name": "夜枭", "model": "ollama/a", "memory_id": "night-owl"})
    assert r.status_code == 201
    a = r.json()
    assert a["agent_id"].startswith("a_") and a["profile"]["name"] == "夜枭" and a["created_at"]
    assert client.post("/api/v1/agents", json={"name": "夜枭", "model": "x"}).status_code == 409
    assert client.post("/api/v1/agents", json={"name": "另", "model": "x", "memory_id": "night-owl"}).status_code == 409
    assert client.post("/api/v1/agents", json={"model": "x"}).status_code == 422  # name 必填
    assert client.post("/api/v1/agents", json={"name": "z", "model": "x", "skills": ["nope"]}).status_code == 400
    bad_p = {"name": "z", "model": "x", "personality": {"description": "上帝视角"}}
    assert client.post("/api/v1/agents", json=bad_p).status_code == 422
    aid = a["agent_id"]
    assert client.get(f"/api/v1/agents/{aid}").json()["profile"]["memory_id"] == "night-owl"
    r = client.put(f"/api/v1/agents/{aid}", json={"name": "夜枭", "model": "ollama/b", "memory_id": "night-owl"})
    assert r.status_code == 200 and r.json()["profile"]["model"] == "ollama/b"
    assert r.json()["updated_at"] >= a["updated_at"]
    assert client.put("/api/v1/agents/a_missing", json={"name": "q", "model": "x"}).status_code == 404
    lst = client.get("/api/v1/agents").json()
    assert [x["agent_id"] for x in lst] == [aid]
    assert client.delete(f"/api/v1/agents/{aid}").status_code == 204
    assert client.delete(f"/api/v1/agents/{aid}").status_code == 404


def test_skills_and_presets(client: TestClient) -> None:
    skills = client.get("/api/v1/skills").json()
    names = {s["name"] for s in skills}
    assert {"logic-chain", "wolf-claim-jump", "seer-badge-flow"} <= names and len(names) >= 14
    s = next(x for x in skills if x["name"] == "wolf-claim-jump")
    assert s["description"] and "WEREWOLF" in s["roles"] and isinstance(s["phases"], list)
    presets = client.get("/api/v1/presets").json()
    assert [p["name"] for p in presets] == [
        "std_12_yn_hunter_idiot", "std_12_yn_hunter_guard", "std_9_kill_side", "std_9_kill_all"
    ]
    nine = next(p for p in presets if p["name"] == "std_9_kill_side")
    assert nine["num_players"] == 9 and nine["sheriff"] is True and nine["win_condition"] == "KILL_SIDE"
    assert sum(r["count"] for r in nine["roles"]) == 9 and nine["description_zh"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_agent_library.py tests/test_api_agents.py -q` → `ModuleNotFoundError: app.runtime.agent_library`。

- [ ] **Step 3: 实现**

`backend/app/runtime/agent_library.py`：

```python
"""Agent 档案库（issue #26）：每个档案一份 JSON（data/agents/<agent_id>.json）。IO 层。

坏文件：list() 跳过并记 warning（一个坏文件不该让整个档案库不可用）；get() 抛 StoreCorruptionError。
"""

from __future__ import annotations

import contextlib
import json
import logging
import os
import tempfile
from pathlib import Path
from typing import Protocol

from pydantic import BaseModel, ConfigDict, ValidationError

from app.agent.profile import AgentProfile
from app.store.event_store import StoreCorruptionError, StoreError

logger = logging.getLogger(__name__)


class StoredAgent(BaseModel):
    model_config = ConfigDict(frozen=True)

    agent_id: str
    profile: AgentProfile
    created_at: str
    updated_at: str


class AgentLibraryStore(Protocol):
    def list(self) -> list[StoredAgent]: ...
    def get(self, agent_id: str) -> StoredAgent | None: ...
    def put(self, stored: StoredAgent) -> None: ...
    def delete(self, agent_id: str) -> bool: ...


class InMemoryAgentLibrary:
    def __init__(self) -> None:
        self._docs: dict[str, StoredAgent] = {}

    def list(self) -> list[StoredAgent]:
        return sorted(self._docs.values(), key=lambda s: s.updated_at, reverse=True)

    def get(self, agent_id: str) -> StoredAgent | None:
        return self._docs.get(agent_id)

    def put(self, stored: StoredAgent) -> None:
        self._docs[stored.agent_id] = stored

    def delete(self, agent_id: str) -> bool:
        return self._docs.pop(agent_id, None) is not None


class JsonFileAgentLibrary:
    def __init__(self, data_dir: Path) -> None:
        self._dir = data_dir  # 首次 put 时创建

    def _path(self, agent_id: str) -> Path:
        return self._dir / f"{agent_id}.json"

    def _read(self, path: Path) -> StoredAgent:
        try:
            raw = json.loads(path.read_text(encoding="utf-8"))
        except OSError as exc:
            raise StoreError(f"读取档案失败：{path}：{exc}") from exc
        except json.JSONDecodeError as exc:
            raise StoreCorruptionError(f"档案不是合法 JSON：{path}：{exc}") from exc
        try:
            return StoredAgent.model_validate(raw)
        except ValidationError as exc:
            raise StoreCorruptionError(f"档案校验失败：{path}：{exc}") from exc

    def list(self) -> list[StoredAgent]:
        if not self._dir.is_dir():
            return []
        out: list[StoredAgent] = []
        for path in sorted(self._dir.glob("*.json")):
            try:
                out.append(self._read(path))
            except StoreCorruptionError as exc:
                logger.warning("跳过坏档案文件 %s：%s", path.name, exc)
        return sorted(out, key=lambda s: s.updated_at, reverse=True)

    def get(self, agent_id: str) -> StoredAgent | None:
        path = self._path(agent_id)
        return self._read(path) if path.exists() else None

    def put(self, stored: StoredAgent) -> None:
        path = self._path(stored.agent_id)
        try:
            self._dir.mkdir(parents=True, exist_ok=True)
            fd, tmp = tempfile.mkstemp(prefix=f".{stored.agent_id}.", suffix=".tmp", dir=self._dir)
            try:
                with os.fdopen(fd, "w", encoding="utf-8") as f:
                    f.write(stored.model_dump_json(indent=2))
                os.replace(tmp, path)
            except BaseException:
                with contextlib.suppress(OSError):
                    os.unlink(tmp)
                raise
        except OSError as exc:
            raise StoreError(f"写入档案失败：{path}：{exc}") from exc

    def delete(self, agent_id: str) -> bool:
        path = self._path(agent_id)
        if not path.exists():
            return False
        try:
            path.unlink()
        except OSError as exc:
            raise StoreError(f"删除档案失败：{path}：{exc}") from exc
        return True
```

`backend/app/schemas/presets.py`：

```python
"""内置板子的展示信息（issue #26 Lobby 步骤 1）。"""

from __future__ import annotations

from pydantic import BaseModel

from app.engine.config import GameConfig

PRESET_DESCRIPTION_ZH: dict[str, str] = {
    "std_9_kill_side": "9 人屠边 · 预女猎 + 3 狼 + 3 民",
    "std_9_kill_all": "9 人屠城 · 预女猎 + 3 狼 + 3 民",
    "std_12_yn_hunter_idiot": "12 人预女猎白 · 4 狼 + 4 民",
    "std_12_yn_hunter_guard": "12 人预女猎守 · 4 狼 + 4 民",
}


class PresetRole(BaseModel):
    role: str
    count: int


class PresetInfo(BaseModel):
    name: str
    num_players: int
    roles: list[PresetRole]
    sheriff: bool
    win_condition: str
    description_zh: str


def preset_info(name: str, config: GameConfig) -> PresetInfo:
    return PresetInfo(
        name=name,
        num_players=config.num_players,
        roles=[PresetRole(role=s.role.value, count=s.count) for s in config.roles],
        sheriff=config.sheriff.enabled,
        win_condition=config.win_condition.value,
        description_zh=PRESET_DESCRIPTION_ZH.get(name, name),
    )
```

`backend/app/api/agents.py`：

```python
"""Agent 档案库 + 建局辅助端点（issue #26）。无鉴权：与建局一致，M3 单用户本地部署。"""

from __future__ import annotations

import secrets
from datetime import UTC, datetime
from typing import Any

from fastapi import APIRouter, Depends, HTTPException, Request, Response

from app.agent.profile import AgentProfile
from app.agent.skills import SkillError, SkillLibrary
from app.engine.config import PRESET_NAMES, build_preset
from app.runtime.agent_library import AgentLibraryStore, StoredAgent
from app.schemas.presets import PresetInfo, preset_info

router = APIRouter()


def get_library(request: Request) -> AgentLibraryStore:
    lib: AgentLibraryStore = request.app.state.agent_library
    return lib


def get_skill_library(request: Request) -> SkillLibrary:
    lib: SkillLibrary = request.app.state.games.skill_library
    return lib


def _now() -> str:
    return datetime.now(UTC).isoformat(timespec="seconds")


def _check_profile(
    profile: AgentProfile, lib: AgentLibraryStore, skills: SkillLibrary, *, exclude_id: str | None
) -> None:
    """名字必填且唯一；memory_id 唯一；技能名可解析。"""
    if not profile.name:
        raise HTTPException(status_code=422, detail="档案须有名字（name）")
    for other in lib.list():
        if other.agent_id == exclude_id:
            continue
        if other.profile.name == profile.name:
            raise HTTPException(status_code=409, detail=f"已有同名档案：{profile.name}")
        if profile.memory_id and other.profile.memory_id == profile.memory_id:
            raise HTTPException(
                status_code=409,
                detail=f"memory_id {profile.memory_id!r} 已被档案「{other.profile.name}」使用",
            )
    try:
        skills.resolve(profile.skills)
    except SkillError as exc:
        raise HTTPException(status_code=400, detail=str(exc)) from exc


@router.get("/agents")
def list_agents(lib: AgentLibraryStore = Depends(get_library)) -> list[StoredAgent]:
    return lib.list()


@router.post("/agents", status_code=201)
def create_agent(
    profile: AgentProfile,
    lib: AgentLibraryStore = Depends(get_library),
    skills: SkillLibrary = Depends(get_skill_library),
) -> StoredAgent:
    _check_profile(profile, lib, skills, exclude_id=None)
    now = _now()
    stored = StoredAgent(
        agent_id=f"a_{secrets.token_hex(4)}", profile=profile, created_at=now, updated_at=now
    )
    lib.put(stored)
    return stored


@router.get("/agents/{agent_id}")
def get_agent(agent_id: str, lib: AgentLibraryStore = Depends(get_library)) -> StoredAgent:
    stored = lib.get(agent_id)
    if stored is None:
        raise HTTPException(status_code=404, detail=f"档案不存在：{agent_id}")
    return stored


@router.put("/agents/{agent_id}")
def update_agent(
    agent_id: str,
    profile: AgentProfile,
    lib: AgentLibraryStore = Depends(get_library),
    skills: SkillLibrary = Depends(get_skill_library),
) -> StoredAgent:
    existing = lib.get(agent_id)
    if existing is None:
        raise HTTPException(status_code=404, detail=f"档案不存在：{agent_id}")
    _check_profile(profile, lib, skills, exclude_id=agent_id)
    stored = existing.model_copy(update={"profile": profile, "updated_at": _now()})
    lib.put(stored)
    return stored


@router.delete("/agents/{agent_id}", status_code=204, response_class=Response)
def delete_agent(agent_id: str, lib: AgentLibraryStore = Depends(get_library)) -> Response:
    if not lib.delete(agent_id):
        raise HTTPException(status_code=404, detail=f"档案不存在：{agent_id}")
    return Response(status_code=204)


@router.get("/skills")
def list_skills(skills: SkillLibrary = Depends(get_skill_library)) -> list[dict[str, Any]]:
    return [
        {
            "name": s.name,
            "description": s.description,
            "roles": sorted(r.value for r in s.roles),
            "phases": sorted(s.phases),
        }
        for s in (skills.get(n) for n in skills.names())
    ]


@router.get("/presets")
def list_presets() -> list[PresetInfo]:
    return [preset_info(name, build_preset(name)) for name in PRESET_NAMES]
```

`app/engine/config.py`：若无 `PRESET_NAMES`，加 `PRESET_NAMES: tuple[str, ...] = tuple(_PRESETS)`（放 `_PRESETS` 定义之后；顺序即字典顺序）。这是引擎文件里加一个只读常量，不改任何规则逻辑——允许。

`app/main.py`：`create_app(..., agents_dir: Path | None = None, agent_library: AgentLibraryStore | None = None)`；`app.state.agent_library = agent_library or JsonFileAgentLibrary(agents_dir or Path("data/agents"))`；`app.include_router(agents.router, prefix="/api/v1")`；import `from app.api import agents`、`from app.runtime.agent_library import AgentLibraryStore, JsonFileAgentLibrary`。

- [ ] **Step 4: 跑测试确认通过 + 全量**

Run: `uv run pytest tests/test_agent_library.py tests/test_api_agents.py -q` → PASS；四条命令全绿；litellm 守卫通过。

- [ ] **Step 5: Commit**

```bash
git add backend/app/runtime/agent_library.py backend/app/api/agents.py backend/app/schemas/presets.py backend/app/main.py backend/app/engine/config.py backend/tests/test_agent_library.py backend/tests/test_api_agents.py
git commit -m "feat(api): Agent 档案库 CRUD（data/agents）+ /skills + /presets (issue #26)"
```

---

### Task 3: 后端 Provider——存储、端点、凭据注入 LiteLLM 客户端

**Files:**
- Create: `backend/app/agent/provider.py`、`backend/app/runtime/provider_store.py`、`backend/app/runtime/provider_probe.py`、`backend/app/api/providers.py`
- Modify: `backend/app/agent/profile.py`（`provider` 字段；`to_agent_config` 用 `resolve_model`）、`backend/app/agent/llm_client.py`（`LiteLLMInstructorClient(api_base=, api_key=)`）、`backend/app/agent/agent_player.py`（`build_agent_port(..., provider=None)`）、`backend/app/runtime/registry.py`（`GameRegistry(provider_store=)`；`_build_agent_port` 取 provider）、`backend/app/cli/play.py`（`--providers-dir`）、`backend/app/main.py`
- Test: `backend/tests/test_provider.py`、`backend/tests/test_api_providers.py`（新）；`backend/tests/test_agent_profile.py`、`backend/tests/test_registry.py`（追加）

**Interfaces:**
- Produces: `ProviderKind`、`Provider`、`ProviderPublic`、`resolve_model(model, provider) -> str`、`ProviderStore` 协议 + `InMemoryProviderStore` / `JsonFileProviderStore(dir)`、`ProviderProbe` 协议（`test(provider, model)`、`list_models(provider)`）+ `LiteLLMProbe`（默认）；`AgentProfile.provider: str | None`；`LiteLLMInstructorClient(max_retries=2, *, api_base=None, api_key=None)`；`build_agent_port(seat, game_config, profile, *, library=None, experience=None, opponents=None, provider=None)`；`GameRegistry(..., provider_store=None)`；`create_app(providers_dir=None, provider_store=None, provider_probe=None)`；端点见规格 §2c。

设计裁决（相对规格 §2c 的简化，规格附注）：凭据**绑定在每个端口的 `LiteLLMInstructorClient` 实例**上（`build_agent_port` 本就为每个端口新建客户端），不再给 `AgentConfig` / `complete_structured` / `memory.reflect` 加参数——调用链零改动，`ScriptedLLMClient` 不受影响。

- [ ] **Step 1: 写失败测试**

`backend/tests/test_provider.py`：

```python
"""Provider 模型与存储（issue #26）：resolve_model、公开视图不含密钥、文件 0600、坏文件。"""

import os

from app.agent.provider import Provider, ProviderPublic, resolve_model
from app.runtime.provider_store import InMemoryProviderStore, JsonFileProviderStore


def _p(**kw: object) -> Provider:
    base = {
        "provider_id": "p_00000001", "name": "本地 Ollama", "kind": "ollama",
        "api_base": "http://localhost:11434", "api_key": None, "default_model": "qwen2.5:14b",
        "created_at": "t", "updated_at": "t",
    }
    base.update(kw)
    return Provider.model_validate(base)


def test_resolve_model_prefix_and_compat_mode() -> None:
    assert resolve_model("ollama/x", None) == "ollama/x"  # provider 为空：原样（走环境变量）
    assert resolve_model("qwen2.5:14b", _p()) == "ollama/qwen2.5:14b"
    assert resolve_model("gpt-4o-mini", _p(kind="openai", api_base=None)) == "openai/gpt-4o-mini"
    assert resolve_model("qwen-max", _p(kind="openai_compatible", api_base="https://llm/v1")) == "openai/qwen-max"
    assert resolve_model("claude-3-5-haiku", _p(kind="anthropic")) == "anthropic/claude-3-5-haiku"


def test_public_view_hides_key() -> None:
    pub = ProviderPublic.from_provider(_p(api_key="sk-abcdefgh1234"))
    d = pub.model_dump()
    assert "api_key" not in d and d["has_key"] is True and d["key_hint"] == "1234"
    assert ProviderPublic.from_provider(_p()).key_hint is None


def test_store_roundtrip_mode_and_corruption(tmp_path, caplog) -> None:
    d = tmp_path / "providers"
    store = JsonFileProviderStore(d)
    assert store.list() == [] and not d.exists()
    store.put(_p(api_key="sk-secret"))
    path = d / "p_00000001.json"
    assert path.exists() and (path.stat().st_mode & 0o777) == 0o600
    assert "sk-secret" in path.read_text(encoding="utf-8")  # 本地明文，权限 0600（README 明示）
    got = JsonFileProviderStore(d).get("p_00000001")
    assert got is not None and got.api_key == "sk-secret"
    (d / "p_bad.json").write_text("{", encoding="utf-8")
    assert [p.provider_id for p in JsonFileProviderStore(d).list()] == ["p_00000001"]
    assert "p_bad.json" in caplog.text
    mem = InMemoryProviderStore()
    mem.put(_p())
    assert mem.get("p_00000001") is not None and mem.delete("p_00000001") and not mem.delete("p_00000001")
    os.chmod(path, 0o600)
```

`backend/tests/test_api_providers.py`：

```python
"""Provider API（issue #26）：CRUD、密钥语义、引用保护、test/models 经注入探测器。"""

import pytest
from fastapi.testclient import TestClient

from app.agent.provider import Provider
from app.main import create_app
from app.runtime.agent_library import InMemoryAgentLibrary
from app.runtime.provider_store import InMemoryProviderStore
from app.store.event_store import InMemoryEventStore


class FakeProbe:
    def __init__(self) -> None:
        self.calls: list[tuple[str, str | None]] = []

    async def test(self, provider: Provider, model: str | None) -> dict[str, object]:
        self.calls.append(("test", model))
        if provider.kind == "anthropic":
            return {"ok": False, "latency_ms": 318, "error": "AuthenticationError: invalid x-api-key"}
        return {"ok": True, "latency_ms": 142, "error": None}

    async def list_models(self, provider: Provider) -> dict[str, object]:
        self.calls.append(("models", None))
        return {"models": ["qwen2.5:14b", "qwen2.5:7b"], "error": None}


@pytest.fixture()
def env() -> tuple[TestClient, FakeProbe]:
    probe = FakeProbe()
    app = create_app(
        store=InMemoryEventStore(), agent_library=InMemoryAgentLibrary(),
        provider_store=InMemoryProviderStore(), provider_probe=probe,
    )
    return TestClient(app), probe


def test_provider_crud_key_semantics_and_reference_guard(env: tuple[TestClient, FakeProbe]) -> None:
    client, _ = env
    r = client.post("/api/v1/providers", json={"name": "本地 Ollama", "kind": "ollama", "default_model": "qwen2.5:14b"})
    assert r.status_code == 201
    p = r.json()
    assert p["provider_id"].startswith("p_") and p["api_base"] == "http://localhost:11434"  # ollama 默认地址
    assert p["has_key"] is False and p["key_hint"] is None and "api_key" not in p
    assert client.post("/api/v1/providers", json={"name": "本地 Ollama", "kind": "ollama"}).status_code == 409
    assert client.post("/api/v1/providers", json={"name": "x", "kind": "openai_compatible"}).status_code == 422  # 兼容类型 api_base 必填
    r = client.post("/api/v1/providers", json={"name": "OpenAI", "kind": "openai", "api_key": "sk-abcd1234"})
    pid = r.json()["provider_id"]
    assert r.json()["has_key"] is True and r.json()["key_hint"] == "1234"
    # PUT：省略 api_key = 保留；"" = 清除
    r = client.put(f"/api/v1/providers/{pid}", json={"name": "OpenAI", "kind": "openai", "default_model": "gpt-4o-mini"})
    assert r.json()["has_key"] is True and r.json()["default_model"] == "gpt-4o-mini"
    r = client.put(f"/api/v1/providers/{pid}", json={"name": "OpenAI", "kind": "openai", "api_key": ""})
    assert r.json()["has_key"] is False
    assert client.get(f"/api/v1/providers/{pid}").status_code == 200
    assert client.get("/api/v1/providers/p_missing").status_code == 404
    # 被档案引用时 409
    a = client.post("/api/v1/agents", json={"name": "铁齿", "model": "gpt-4o-mini", "provider": pid}).json()
    r = client.delete(f"/api/v1/providers/{pid}")
    assert r.status_code == 409 and "铁齿" in r.json()["detail"]
    assert client.post("/api/v1/agents", json={"name": "q", "model": "m", "provider": "p_missing"}).status_code == 400
    client.delete(f"/api/v1/agents/{a['agent_id']}")
    assert client.delete(f"/api/v1/providers/{pid}").status_code == 204
    assert [x["name"] for x in client.get("/api/v1/providers").json()] == ["本地 Ollama"]


def test_provider_test_and_models_use_probe(env: tuple[TestClient, FakeProbe]) -> None:
    client, probe = env
    pid = client.post("/api/v1/providers", json={"name": "本地 Ollama", "kind": "ollama", "default_model": "qwen2.5:14b"}).json()["provider_id"]
    r = client.post(f"/api/v1/providers/{pid}/test", json={})
    assert r.status_code == 200 and r.json() == {"ok": True, "latency_ms": 142, "error": None}
    assert probe.calls[-1] == ("test", "qwen2.5:14b")
    r = client.post(f"/api/v1/providers/{pid}/test", json={"model": "llama3.1:8b"})
    assert probe.calls[-1] == ("test", "llama3.1:8b")
    r = client.get(f"/api/v1/providers/{pid}/models")
    assert r.status_code == 200 and r.json()["models"] == ["qwen2.5:14b", "qwen2.5:7b"]
    aid = client.post("/api/v1/providers", json={"name": "Anthropic", "kind": "anthropic", "api_key": "bad"}).json()["provider_id"]
    r = client.post(f"/api/v1/providers/{aid}/test", json={"model": "claude-3-5-haiku"})
    assert r.status_code == 200 and r.json()["ok"] is False and "AuthenticationError" in r.json()["error"]
    assert client.post(f"/api/v1/providers/{aid}/test", json={}).status_code == 400  # 无 default_model 且未指定 model
```

追加到 `backend/tests/test_agent_profile.py`：

```python
def test_profile_provider_field_and_to_agent_config_resolves_model() -> None:
    from app.agent.provider import Provider
    from app.agent.profile import to_agent_config
    from app.engine.config import build_preset

    p = Provider(
        provider_id="p_1", name="本地 Ollama", kind="ollama", api_base="http://localhost:11434",
        api_key=None, default_model=None, created_at="t", updated_at="t",
    )
    prof = AgentProfile(model="qwen2.5:14b", model_speech="qwen2.5:7b", provider="p_1")
    cfg = to_agent_config(prof, build_preset("std_9_kill_side"), provider=p)
    assert cfg.model == "ollama/qwen2.5:14b" and cfg.model_speech == "ollama/qwen2.5:7b"
    assert to_agent_config(AgentProfile(model="openai/x"), build_preset("std_9_kill_side")).model == "openai/x"
    assert "provider" in AgentProfile(model="m").model_dump()
```

追加到 `backend/tests/test_registry.py`：

```python
def test_registry_passes_provider_to_agent_port_factory_context() -> None:
    """registry 在 start() 时按 profile.provider 取 provider 并放到 handle.providers 供工厂使用。"""
    from app.agent.profile import AgentProfile
    from app.agent.provider import Provider
    from app.runtime.player_port import BotPlayerPort
    from app.runtime.provider_store import InMemoryProviderStore

    ps = InMemoryProviderStore()
    ps.put(Provider(provider_id="p_1", name="o", kind="ollama", api_base="http://x", api_key="k", default_model=None, created_at="t", updated_at="t"))
    seen: dict[int, object] = {}

    def factory(seat: int, h: GameHandle) -> PlayerPort:
        seen[seat] = h.providers.get(seat)
        return BotPlayerPort(state_provider=h.live_state)

    reg = GameRegistry(InMemoryEventStore(), RunnerTimeouts(speech_sec=5.0, action_sec=5.0), agent_port_factory=factory, provider_store=ps)
    cfg = build_preset("std_9_kill_side").model_copy(update={"seed": 5})
    handle = reg.create(cfg, allow_spectators=False, agents={"0": AgentProfile(model="q", provider="p_1"), "*": AgentProfile(model="ollama/z")})
    reg.start(handle)
    assert getattr(seen[0], "provider_id", None) == "p_1" and seen[1] is None
    with pytest.raises(ValueError, match="provider"):
        reg.create(cfg, allow_spectators=False, agents={"0": AgentProfile(model="q", provider="p_missing")})
    assert handle.task is not None
    handle.task.cancel()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_provider.py tests/test_api_providers.py tests/test_agent_profile.py tests/test_registry.py -q -k "provider"` → `ModuleNotFoundError: app.agent.provider` 等。

- [ ] **Step 3: 实现**

`backend/app/agent/provider.py`：

```python
"""模型服务 Provider（issue #26）：地址 / 密钥 / 默认模型；纯 pydantic。

kind 即 LiteLLM 模型前缀（openai_compatible → "openai" 前缀 + 自定义 api_base）。
凭据在 build_agent_port 时绑定到该端口的 LiteLLMInstructorClient，不进 AgentConfig / 事件日志。
"""

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, model_validator

ProviderKind = Literal["ollama", "openai", "anthropic", "openai_compatible", "gemini", "deepseek"]

_PREFIX: dict[str, str] = {
    "ollama": "ollama",
    "openai": "openai",
    "anthropic": "anthropic",
    "openai_compatible": "openai",
    "gemini": "gemini",
    "deepseek": "deepseek",
}
DEFAULT_API_BASE: dict[str, str | None] = {
    "ollama": "http://localhost:11434",
    "openai": None,
    "anthropic": None,
    "openai_compatible": None,  # 必填
    "gemini": None,
    "deepseek": "https://api.deepseek.com",
}


class ProviderInput(BaseModel):
    """POST/PUT 请求体；PUT 时 api_key 省略 = 保留原密钥、"" = 清除。"""

    model_config = ConfigDict(extra="forbid")

    name: str
    kind: ProviderKind
    api_base: str | None = None
    api_key: str | None = None
    default_model: str | None = None

    @model_validator(mode="after")
    def _fill_and_check(self) -> ProviderInput:
        self.name = self.name.strip()
        if not self.name:
            raise ValueError("name 不能为空")
        if self.api_base is None:
            self.api_base = DEFAULT_API_BASE[self.kind]
        if self.kind == "openai_compatible" and not self.api_base:
            raise ValueError("openai_compatible 类型须填写 api_base")
        return self


class Provider(BaseModel):
    model_config = ConfigDict(frozen=True)

    provider_id: str
    name: str
    kind: ProviderKind
    api_base: str | None
    api_key: str | None
    default_model: str | None
    created_at: str
    updated_at: str


class ProviderPublic(BaseModel):
    """API 响应：永不含 api_key。"""

    provider_id: str
    name: str
    kind: ProviderKind
    api_base: str | None
    has_key: bool
    key_hint: str | None
    default_model: str | None
    created_at: str
    updated_at: str

    @classmethod
    def from_provider(cls, p: Provider) -> ProviderPublic:
        return cls(
            provider_id=p.provider_id,
            name=p.name,
            kind=p.kind,
            api_base=p.api_base,
            has_key=bool(p.api_key),
            key_hint=p.api_key[-4:] if p.api_key else None,
            default_model=p.default_model,
            created_at=p.created_at,
            updated_at=p.updated_at,
        )


def resolve_model(model: str, provider: Provider | None) -> str:
    """provider 为空 → 原样（含前缀、走环境变量）；否则拼 LiteLLM 前缀。"""
    if provider is None:
        return model
    return f"{_PREFIX[provider.kind]}/{model}"
```

`backend/app/runtime/provider_store.py`：与 `agent_library.py` 同结构（`ProviderStore` 协议 `list/get/put/delete`，`InMemoryProviderStore`，`JsonFileProviderStore(dir)`：`put` 写完 `os.chmod(path, 0o600)`；`list` 跳过坏文件记 warning；`get` 坏文件抛 `StoreCorruptionError`）。文件名 `<provider_id>.json`。

`backend/app/runtime/provider_probe.py`：

```python
"""Provider 探测（issue #26）：测试连接与拉取模型列表。默认实现在函数内 import litellm / httpx。"""

from __future__ import annotations

import time
from typing import Any, Protocol

from app.agent.provider import Provider, resolve_model


class ProviderProbe(Protocol):
    async def test(self, provider: Provider, model: str | None) -> dict[str, Any]: ...
    async def list_models(self, provider: Provider) -> dict[str, Any]: ...


_STATIC_MODELS: dict[str, list[str]] = {
    "anthropic": ["claude-3-5-haiku-latest", "claude-3-5-sonnet-latest"],
    "gemini": ["gemini-1.5-flash", "gemini-1.5-pro"],
}


class LiteLLMProbe:
    async def test(self, provider: Provider, model: str | None) -> dict[str, Any]:
        import litellm  # 惰性

        name = model or provider.default_model
        assert name is not None  # 端点已校验
        extra: dict[str, Any] = {}
        if provider.api_base:
            extra["api_base"] = provider.api_base
        if provider.api_key:
            extra["api_key"] = provider.api_key
        t0 = time.monotonic()
        try:
            await litellm.acompletion(
                model=resolve_model(name, provider),
                messages=[{"role": "user", "content": "ping"}],
                max_tokens=8,
                timeout=5,
                **extra,
            )
        except Exception as exc:  # 错误原文回给 UI
            return {"ok": False, "latency_ms": int((time.monotonic() - t0) * 1000), "error": f"{type(exc).__name__}: {exc}"}
        return {"ok": True, "latency_ms": int((time.monotonic() - t0) * 1000), "error": None}

    async def list_models(self, provider: Provider) -> dict[str, Any]:
        if provider.kind in _STATIC_MODELS:
            return {"models": _STATIC_MODELS[provider.kind], "error": None}
        import httpx  # 惰性

        base = (provider.api_base or "").rstrip("/")
        try:
            async with httpx.AsyncClient(timeout=5) as http:
                if provider.kind == "ollama":
                    r = await http.get(f"{base}/api/tags")
                    r.raise_for_status()
                    return {"models": [m["name"] for m in r.json().get("models", [])], "error": None}
                headers = {"Authorization": f"Bearer {provider.api_key}"} if provider.api_key else {}
                url = f"{base}/v1/models" if not base.endswith("/v1") else f"{base}/models"
                if provider.kind == "openai" and not base:
                    url = "https://api.openai.com/v1/models"
                r = await http.get(url, headers=headers)
                r.raise_for_status()
                return {"models": sorted(m["id"] for m in r.json().get("data", [])), "error": None}
        except Exception as exc:
            return {"models": [], "error": f"{type(exc).__name__}: {exc}"}
```

`backend/app/api/providers.py`：端点按规格 §2c 表——`get_provider_store` / `get_probe` 依赖从 `request.app.state`；`POST` 生成 `p_` + `token_hex(4)`，名字唯一 409；`PUT` 密钥语义（`api_key` 字段缺省——用 `ProviderInput` 的 `model_fields_set` 判断是否给了 `api_key`：未给 → 保留，`""` → None）；`DELETE` 先扫 `agent_library.list()` 找 `profile.provider == id` 的档案，有则 409 `detail=f"仍被 {n} 个 Agent 引用：{名字…}"`；`POST /{id}/test` body `{model?: str}`，无 `model` 且 provider 无 `default_model` → 400；`GET /{id}/models` → probe。响应统一 `ProviderPublic`。

`backend/app/agent/profile.py`：`provider: str | None = None  # 模型服务 id（issue #26）；None = model 含前缀、凭据走环境变量`；`to_agent_config(profile, game_config, provider: Provider | None = None)`：`model=resolve_model(profile.model, provider)`、`model_speech` / `reflection_model` 非空时同样 resolve；`validate_profiles(agents, num_players, library=None, providers: Collection[str] | None = None)`：给了 `providers` 时，`profile.provider` 非空且不在集合 → `ValueError(f"档案引用的 provider 不存在：{profile.provider!r}")`。`from app.agent.provider import Provider, resolve_model`（纯 pydantic，安全）。

`backend/app/agent/llm_client.py`：`LiteLLMInstructorClient.__init__(self, max_retries: int = 2, *, api_base: str | None = None, api_key: str | None = None)`；`complete_structured` 里 `if self._api_base: extra["api_base"] = ...`、`if self._api_key: extra["api_key"] = ...`。

`backend/app/agent/agent_player.py` `build_agent_port(..., provider: Provider | None = None)`：`to_agent_config(profile, game_config, provider=provider)`；`client=LiteLLMInstructorClient(api_base=provider.api_base if provider else None, api_key=provider.api_key if provider else None)`。

`backend/app/runtime/registry.py`：`GameRegistry(..., provider_store: ProviderStore | None = None)`（默认 `InMemoryProviderStore()`）；`create()` 里 `validate_profiles(resolved, n, self.skill_library, providers={p.provider_id for p in self._provider_store.list()})`；`GameHandle.providers: dict[int, Provider] = {}`；`start()` 在 load experiences 处同样 `for seat, profile in 座位档案: if profile.provider: handle.providers[seat] = store.get(profile.provider)`（None 视为已在 create 校验过；仍 None → `LobbyError("provider 已被删除")`）；`_build_agent_port` 传 `provider=handle.providers.get(seat)`。

`backend/app/cli/play.py`：`--providers-dir`（默认 `data/providers`）；`main` 里 `providers = JsonFileProviderStore(Path(args.providers_dir))`，`validate_profiles(..., providers={...ids})`，`_wire_game(..., providers=providers)` 内按 `profile.provider` 取 provider 传 `build_agent_port`。`bench.py` 同样加 `--providers-dir` 并透传（两行）。

`backend/app/main.py`：`create_app(..., providers_dir=None, provider_store=None, provider_probe=None)`；`app.state.provider_store = provider_store or JsonFileProviderStore(providers_dir or Path("data/providers"))`；`app.state.provider_probe = provider_probe or LiteLLMProbe()`（`LiteLLMProbe` 模块级 import 安全：litellm 在方法内 import）；`GameRegistry(provider_store=app.state.provider_store)`；`include_router(providers.router, prefix="/api/v1")`。

- [ ] **Step 4: 跑测试确认通过 + 全量**

四条命令全绿；litellm 守卫通过；`tests/test_agent_bench.py` 冒烟不受影响（不设 provider）。

- [ ] **Step 5: Commit**

```bash
git add backend/app backend/tests
git commit -m "feat(api): 模型服务 Provider——存储（0600）、CRUD/test/models 端点、AgentProfile.provider、凭据绑定端口客户端 (issue #26)"
```

---

### Task 4: 金样导出 CLI、`make fixtures`、静态挂载

**Files:**
- Create: `backend/app/cli/export_fixture.py`
- Modify: `backend/app/main.py`（`StaticFiles` 挂载）、`Makefile`（`fixtures`、`fe-*` 目标）、`.gitignore`（`frontend/node_modules`、`frontend/dist`）
- Test: `backend/tests/test_export_fixture.py`（新）、`backend/tests/test_api_lobby.py`（追加静态挂载）

**Interfaces:**
- Produces: `export_fixture(preset, seed) -> dict`（`{"preset","seed","meta","events","states"}`）；`main(argv)`；`create_app(frontend_dist: Path | None = None)`（默认 `<repo>/frontend/dist`，存在则挂 `/`）。

- [ ] **Step 1: 写失败测试**

`backend/tests/test_export_fixture.py`：

```python
import json

from app.cli.export_fixture import export_fixture, main
from app.engine.events import Event, reduce_all
from app.store.event_store import GameMeta, initial_state


def test_fixture_states_match_prefix_reduce_and_sets_sorted() -> None:
    doc = export_fixture("std_9_kill_side", 3)
    assert doc["preset"] == "std_9_kill_side" and doc["seed"] == 3
    meta = GameMeta.model_validate(doc["meta"])
    events = [Event.model_validate(e) for e in doc["events"]]  # SerializeAsAny 往返（#34）
    assert len(doc["states"]) == len(events) and doc["states"][-1]["phase"] == "GAME_OVER"
    for i in (0, 5, len(events) // 2, len(events) - 1):
        expected = reduce_all(initial_state(meta), events[: i + 1]).model_dump(mode="json")
        got = doc["states"][i]
        for k in ("acted_seats", "sheriff_declared", "sheriff_withdrawn", "sheriff_confirmed"):
            assert got[k] == sorted(got[k])
            expected[k] = sorted(expected[k])
        assert got == expected
    assert doc["meta"]["agents"] == {}


def test_main_writes_file(tmp_path) -> None:
    out = tmp_path / "f.json"
    main(["--preset", "std_9_kill_all", "--seed", "3", "--out", str(out)])
    doc = json.loads(out.read_text(encoding="utf-8"))
    assert doc["preset"] == "std_9_kill_all" and doc["events"][0]["type"] == "GAME_CREATED"
```

追加到 `backend/tests/test_api_lobby.py`：

```python
def test_frontend_dist_mounted_only_when_present(tmp_path) -> None:
    from app.main import create_app
    from app.store.event_store import InMemoryEventStore

    c = TestClient(create_app(store=InMemoryEventStore(), frontend_dist=tmp_path / "nope"))
    assert c.get("/").status_code == 404
    dist = tmp_path / "dist"
    dist.mkdir()
    (dist / "index.html").write_text("<h1>AgentHowl</h1>", encoding="utf-8")
    c = TestClient(create_app(store=InMemoryEventStore(), frontend_dist=dist))
    assert c.get("/").status_code == 200 and "AgentHowl" in c.get("/").text
    assert c.get("/api/v1/presets").status_code == 200  # API 前缀不受影响
```

- [ ] **Step 2: 跑测试确认失败** → `ModuleNotFoundError` / `TypeError: frontend_dist`。

- [ ] **Step 3: 实现**

`backend/app/cli/export_fixture.py`：

```python
"""前端 reducer 金样导出（issue #26）：meta + 全部事件 + 每条事件后的完整状态。零 LLM。"""

from __future__ import annotations

import argparse
import json
from pathlib import Path
from typing import Any

from app.cli.bot import run_game
from app.engine.config import build_preset
from app.engine.events import reduce
from app.store.event_store import GameMeta, SeatName, event_to_json, initial_state

_SET_FIELDS = ("acted_seats", "sheriff_declared", "sheriff_withdrawn", "sheriff_confirmed")


def _state_json(state: Any) -> dict[str, Any]:
    d: dict[str, Any] = state.model_dump(mode="json")
    for k in _SET_FIELDS:  # frozenset 序列化顺序不保证：统一升序（规格 §3）
        d[k] = sorted(d[k])
    return d


def export_fixture(preset: str, seed: int) -> dict[str, Any]:
    config = build_preset(preset).model_copy(update={"seed": seed})
    final, events = run_game(config, game_id=f"fx-{preset}-{seed}")
    roster = tuple(SeatName(seat=p.seat, display_name=p.display_name) for p in final.players)
    meta = GameMeta(game_id=final.game_id, config=config, roster=roster)
    state = initial_state(meta)
    states: list[dict[str, Any]] = []
    for e in events:
        state = reduce(state, e)
        states.append(_state_json(state))
    return {
        "preset": preset,
        "seed": seed,
        "meta": meta.model_dump(mode="json"),
        "events": [event_to_json(e) for e in events],
        "states": states,
    }


def main(argv: list[str] | None = None) -> None:
    parser = argparse.ArgumentParser(prog="app.cli.export_fixture")
    parser.add_argument("--preset", default="std_9_kill_side")
    parser.add_argument("--seed", type=int, default=3)
    parser.add_argument("--out", required=True)
    args = parser.parse_args(argv)
    doc = export_fixture(args.preset, args.seed)
    out = Path(args.out)
    out.parent.mkdir(parents=True, exist_ok=True)
    out.write_text(json.dumps(doc, ensure_ascii=False, separators=(",", ":")), encoding="utf-8")
    print(f"{out}: {len(doc['events'])} events")


if __name__ == "__main__":
    main()
```

`backend/app/main.py`：`create_app(..., frontend_dist: Path | None = None)`；在 `return app` 前：
```python
    # 前端产物（issue #26）：存在则整站挂到 /；API 路由已先注册，/api/v1 不受影响
    dist = frontend_dist if frontend_dist is not None else Path(__file__).resolve().parents[2] / "frontend" / "dist"
    if dist.is_dir():
        from fastapi.staticfiles import StaticFiles

        app.mount("/", StaticFiles(directory=str(dist), html=True), name="frontend")
```

`Makefile`：变量区 `FRONTEND := frontend`；目标：
```make
.PHONY: fixtures
fixtures: ## 生成前端 reducer 金样（4 个 preset × seed 3）到 frontend/src/engine/__fixtures__/
	cd $(BACKEND) && for p in std_9_kill_side std_9_kill_all std_12_yn_hunter_idiot std_12_yn_hunter_guard; do \
		$(UV) python -m app.cli.export_fixture --preset $$p --seed 3 --out ../$(FRONTEND)/src/engine/__fixtures__/$$p-3.json; done

.PHONY: fe-install fe-dev fe-check fe-build
fe-install: ## 前端依赖（npm ci）
	cd $(FRONTEND) && npm ci
fe-dev: ## 前端开发服务器（Vite，/api 代理到 8000）
	cd $(FRONTEND) && npm run dev
fe-check: ## 前端 lint + tsc + vitest
	cd $(FRONTEND) && npm run check
fe-build: ## 前端构建到 frontend/dist（后端存在该目录时自动挂载）
	cd $(FRONTEND) && npm run build
```
`check` 目标末尾追加 `@test -d $(FRONTEND)/node_modules && $(MAKE) fe-check || echo "(frontend 未安装，跳过 fe-check)"`。删除顶部「前端命令待 M3 补充」注释。`.gitignore` 加 `frontend/node_modules/`、`frontend/dist/`。

- [ ] **Step 4: 跑测试确认通过 + 全量**；然后 `make fixtures`（此时 `frontend/src/engine/__fixtures__/` 目录会被创建，4 个 JSON 入库——Task 5 使用）。查看体积 `du -sh frontend/src/engine/__fixtures__`（预期 ≤ 4MB）。

- [ ] **Step 5: Commit**

```bash
git add backend/app/cli/export_fixture.py backend/app/main.py backend/tests/test_export_fixture.py backend/tests/test_api_lobby.py Makefile .gitignore frontend/src/engine/__fixtures__
git commit -m "feat(cli): reducer 金样导出 + make fixtures；frontend/dist 静态挂载；Makefile 前端目标 (issue #26)"
```

---

### Task 5: 前端脚手架 + TS 引擎（types / reduce / normalize / phases / select）+ 金样测试

**Files:**
- Create: `frontend/package.json`、`frontend/package-lock.json`（`npm install` 生成）、`frontend/index.html`、`frontend/vite.config.ts`、`frontend/tsconfig.json`、`frontend/tsconfig.node.json`、`frontend/.eslintrc.cjs`、`frontend/.prettierrc`、`frontend/.nvmrc`、`frontend/src/main.tsx`、`frontend/src/App.tsx`（占位路由）、`frontend/src/vite-env.d.ts`、`frontend/src/styles/tokens.css`（= `docs/design/nocturne.css` + 语义色）、`frontend/src/styles/global.css`、`frontend/src/engine/{types.ts, reduce.ts, normalize.ts, phases.ts, select.ts}`、`frontend/src/engine/{reduce.test.ts, select.test.ts}`

**Interfaces:**
- Produces: 全部引擎类型与函数（规格 §5）；`npm run check` 通过。

- [ ] **Step 1: 脚手架**

```bash
cd frontend  # 新建目录
cat > package.json <<'EOF'
{
  "name": "agenthowl-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .ts,.tsx --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run lint && npm run typecheck && npm run test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "@vitejs/plugin-react": "^4.3.1",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^4.6.2",
    "eslint-plugin-react-refresh": "^0.4.11",
    "jsdom": "^25.0.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
EOF
echo 20 > .nvmrc
npm install   # 生成 package-lock.json；若离线失败，报告 BLOCKED
```

`vite.config.ts`：
```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: { proxy: { "/api": { target: "http://localhost:8000", ws: true, changeOrigin: true } } },
  build: { outDir: "dist" },
  test: { environment: "jsdom", setupFiles: ["./src/test-setup.ts"], globals: false },
});
```
（`/// <reference types="vitest" />` 置顶；`src/test-setup.ts` 内容 `import "@testing-library/jest-dom/vitest";`。）

`tsconfig.json`：`{"compilerOptions": {"target": "ES2022", "lib": ["ES2022", "DOM", "DOM.Iterable"], "module": "ESNext", "moduleResolution": "Bundler", "jsx": "react-jsx", "strict": true, "noUncheckedIndexedAccess": true, "noUnusedLocals": true, "noUnusedParameters": true, "resolveJsonModule": true, "isolatedModules": true, "skipLibCheck": true, "types": ["vitest/globals"]}, "include": ["src", "vite.config.ts"]}`（`types` 可省略若不用 globals）。`.eslintrc.cjs`：`extends: ["eslint:recommended", "plugin:@typescript-eslint/recommended", "plugin:react-hooks/recommended"]`，`parser: "@typescript-eslint/parser"`，`plugins: ["react-refresh"]`，`ignorePatterns: ["dist"]`。`.prettierrc`：`{"printWidth": 100}`。`index.html`：`<div id="root"></div>` + `<script type="module" src="/src/main.tsx">`，`<html lang="zh-CN">`，`<title>AgentHowl</title>`。`src/main.tsx`：`createRoot(...).render(<StrictMode><App/></StrictMode>)`，import `./styles/tokens.css`、`./styles/global.css`。`src/App.tsx`：暂时渲染 `<h1>AgentHowl</h1>`（Task 7/8 替换）。`tokens.css`：复制 `docs/design/nocturne.css` 全文，并在 `:root` 追加：
```css
  --ah-wolf: #E5484D; --ah-god: #F5B041; --ah-villager: #9AA4B2;
  --ah-line-guard: #3B82F6; --ah-line-save: #34C38F; --ah-line-poison: #A855F7; --ah-line-seer: #EAB308;
  --ah-ok: #34C38F; --ah-warn: #F5B041; --ah-err: #E5484D;
  --ah-tick-night: #2b2741; --ah-tick-day: #3f424d;
```
`global.css`：`html,body,#root{height:100%}`、`body{background:var(--color-bg)}`、`.ah-rule-x`/`.ah-rule-y`（两端渐隐分隔线的两种方向，作为可复用类）、`@keyframes ah-pulse`/`ah-spin`（取自 mockup 头部 `<style>`）。

- [ ] **Step 2: 写失败测试（金样）**

`src/engine/reduce.test.ts`：

```ts
import { describe, expect, it } from "vitest";
import { initialState, reduce, reduceAll } from "./reduce";
import { normalizeState } from "./normalize";
import type { Event, GameMeta, GameState } from "./types";
import f9s from "./__fixtures__/std_9_kill_side-3.json";
import f9a from "./__fixtures__/std_9_kill_all-3.json";
import f12i from "./__fixtures__/std_12_yn_hunter_idiot-3.json";
import f12g from "./__fixtures__/std_12_yn_hunter_guard-3.json";

interface Fixture { preset: string; seed: number; meta: GameMeta; events: Event[]; states: GameState[] }
const FIXTURES = [f9s, f9a, f12i, f12g] as unknown as Fixture[];

describe("reduce 与后端逐事件等价（金样）", () => {
  for (const fx of FIXTURES) {
    it(`${fx.preset} seed=${fx.seed}`, () => {
      let s = initialState(fx.meta);
      fx.events.forEach((e, i) => {
        s = reduce(s, e);
        const got = normalizeState(s);
        const want = normalizeState(fx.states[i]!);
        if (JSON.stringify(got) !== JSON.stringify(want)) {
          const diff = Object.keys(want).filter((k) => JSON.stringify((got as Record<string, unknown>)[k]) !== JSON.stringify((want as Record<string, unknown>)[k]));
          throw new Error(`seq=${e.seq} type=${e.type} 不一致字段：${diff.join(",")}`);
        }
      });
      expect(s.phase).toBe("GAME_OVER");
      expect(normalizeState(reduceAll(initialState(fx.meta), fx.events))).toEqual(normalizeState(fx.states.at(-1)!));
    });
  }
  it("未知事件类型抛错", () => {
    const fx = FIXTURES[0]!;
    const bad = { ...fx.events[0]!, type: "NOPE" } as unknown as Event;
    expect(() => reduce(initialState(fx.meta), bad)).toThrow(/未知事件类型/);
  });
  it("normalizeState 幂等", () => {
    const st = FIXTURES[0]!.states[10]!;
    expect(normalizeState(normalizeState(st))).toEqual(normalizeState(st));
  });
});
```

`src/engine/select.test.ts`：在 `std_9_kill_side-3` 上：`aliveSeats(finalState).length === finalState.players.filter(p=>p.alive).length`；`wolfSeats` 为 3 个；找到首个 `VOTE_RESULT` 事件，`voteTally(events.slice(0, idx+1))` 的 `tally` 与该 `VOTE_RESULT.payload.tally` 一致（按座位映射）；`speechItems(events)` 中发言项数 = `PLAYER_SPOKE` 数；`nightSummary(events, 1)` 至少含狼队决定行（GM 事件存在于金样）。

- [ ] **Step 3: 实现引擎**

`src/engine/types.ts`——按规格 §5 逐一定义（字段名与后端相同）。关键片段：

```ts
export type Phase = "LOBBY" | "ROLE_ASSIGN" | "NIGHT_GUARD" | "NIGHT_WEREWOLF" | "NIGHT_WITCH" | "NIGHT_SEER" | "NIGHT_HUNTER_CONFIRM" | "WIN_CHECK" | "SHERIFF_ELECTION" | "SHERIFF_PK" | "DEATH_ANNOUNCE" | "LAST_WORDS" | "DAY_SPEECH" | "VOTE" | "VOTE_PK" | "EXILE" | "HUNTER_SHOOT" | "IDIOT_FLIP" | "GAME_OVER";
export type ElectionStage = "" | "candidacy" | "speech" | "withdraw" | "vote" | "direction" | "announce";
export type RoleType = "WEREWOLF" | "VILLAGER" | "SEER" | "WITCH" | "HUNTER" | "GUARD" | "IDIOT";
export type Faction = "GOOD" | "WOLF";
export type Visibility = "PUBLIC" | "WOLVES" | "ROLE_SELF" | "GM_ONLY";
export type EventType = /* 33 个字面量 */;
export interface Player { seat: number; display_name: string; player_type: "HUMAN" | "AGENT"; role: RoleType; faction: Faction; alive: boolean; is_sheriff: boolean; idiot_revealed: boolean; can_vote: boolean; witch_antidote: boolean; witch_poison: boolean; hunter_can_shoot: boolean; last_guard_target: number | null }
export interface NightActions { guard_target: number | null; wolf_target: number | null; witch_save: boolean; witch_poison_target: number | null; seer_check: number | null }
export interface GameState { game_id: string; config: GameConfigLite; phase: Phase; round: number; players: Player[]; sheriff_seat: number | null; seer_log: Record<string, { round: number; seat: number; result: string }[]>; speech_order: number[]; speech_idx: number; votes: Record<string, number | null>; vote_candidates: number[]; tie_round: number; pending_night: NightActions; wolf_proposals: Record<string, number | null>; wolf_kill_round: number; wolf_proposal_history: [number, number | null][][]; acted_seats: number[]; night_deaths: number[]; resolved_first_night: boolean; pending_hunter: number | null; day_exiled: number | null; winner: string | null; state_version: number; resume_token: string | null; skip_day: boolean; sheriff_candidates: number[]; sheriff_declared: number[]; sheriff_votes: Record<string, number | null>; election_stage: ElectionStage; sheriff_withdrawn: number[]; sheriff_confirmed: number[]; sheriff_speech_direction: string | null; badge_flow_claims: Record<string, number[]> }
export interface GameConfigLite { config_id?: string; num_players: number; sheriff: { enabled: boolean } & Record<string, unknown>; roles: { role: RoleType; count: number }[]; win_condition: string; [k: string]: unknown }
export interface Event<P = EventPayload> { seq: number; game_id: string; ts: number; type: EventType; actor_seat: number | null; payload: P; visibility: Visibility; meta: Record<string, string> }
export interface GameMeta { game_id: string; config: GameConfigLite; roster: { seat: number; display_name: string }[]; agents: Record<string, unknown> }
```
`config` 里 `GameState.config` 在金样中是完整 `GameConfig` JSON——TS 只声明必要字段 + 索引签名，`reduce` 不改它；`normalizeState` 比较时保留原对象即可（金样与 `initialState(meta)` 的 `config` 同源）。

`src/engine/reduce.ts`——`initialState(meta)`：`players = roster.map(r => ({seat, display_name, player_type: "AGENT", role: "VILLAGER", faction: "GOOD", alive: true, is_sheriff: false, idiot_revealed: false, can_vote: true, witch_antidote: true, witch_poison: true, hunter_can_shoot: true, last_guard_target: null}))`，其余字段默认值与 `state.py` 一致（`pending_night` 全 null / `witch_save:false`，`wolf_kill_round: 1`，`tie_round: 0`，`election_stage: ""`，`state_version: 0`）；`reduce(state, e)`：`const next = {...state, state_version: state.state_version + 1}; switch (e.type) { … }` 逐分支实现 `events.py::reduce`（集合用 `addSorted(arr, x)` / `arr.filter`；`_replace_player`；`SHERIFF_CANDIDACY`/`SHERIFF_WITHDREW` 在 `state.election_stage === "withdraw"` 时并入 `sheriff_confirmed`；`ELECTION_STAGE_CHANGED` `stage==="withdraw"` → `sheriff_confirmed=[]`，`skip_day` 置位；`PHASE_CHANGED` 写 `resume_token`（含 null）、`pending_hunter` 仅非 null；`ROUND_STARTED` 重置列表含 `skip_day:false`；`GAME_OVER` `resume_token:null`；`default: throw new Error(\`未知事件类型：${e.type}\`)`）。注意 `player_type` 与 `display_name` 来自 roster，后端 `initial_state` 同样。

`src/engine/normalize.ts`：`normalizeState(s)`：深拷贝后对四个集合字段升序、`seer_log`/`votes`… 的键保持字符串、`undefined` → `null`。

`src/engine/phases.ts`：`PHASE_ZH: Record<Phase, string>`（`NIGHT_GUARD` 夜晚 · 守卫行动、`NIGHT_WEREWOLF` 夜晚 · 狼人行动、`NIGHT_WITCH` 夜晚 · 女巫行动、`NIGHT_SEER` 夜晚 · 预言家行动、`NIGHT_HUNTER_CONFIRM` 夜晚 · 猎人确认、`SHERIFF_ELECTION` 白天 · 警长竞选、`SHERIFF_PK` 白天 · 警长 PK、`DEATH_ANNOUNCE` 白天 · 公布死讯、`LAST_WORDS` 白天 · 遗言、`DAY_SPEECH` 白天 · 发言、`VOTE` 白天 · 投票、`VOTE_PK` 白天 · PK 投票、`EXILE` 白天 · 放逐、`HUNTER_SHOOT` 猎人开枪、`IDIOT_FLIP` 白痴翻牌、`WIN_CHECK` 胜负判定、`GAME_OVER` 游戏结束、`LOBBY` 大厅、`ROLE_ASSIGN` 发牌）、`ELECTION_STAGE_ZH`（与 `render.py` 相同）、`ROLE_ZH`、`ROLE_ABBR`（狼/预/巫/猎/守/痴/民）、`BADGE_LOST_ZH`、`WINNER_ZH`、`isNight(phase)`、`factionColorVar(role)`（`--ah-wolf` / `--ah-god` / `--ah-villager`）。

`src/engine/select.ts`：`aliveSeats`、`wolfSeats`、`currentSpeaker`、`voteTally(events)`（扫最后一个 `VOTE_STARTED` 之后的 `VOTE_CAST`；权重：警长 1.5 需从 state 知道 `sheriff_seat`——签名 `voteTally(state, events)`）、`sheriffVoteTally(state, events)`、`nightSummary(events, round)`（该轮 `ROUND_STARTED` 后到下一个 `ROUND_STARTED` 前的 GUARD_PROTECTED / WOLF_KILL_PROPOSED / REVOTE / DECIDED / WITCH_SAVED / WITCH_POISONED / SEER_CHECKED / NIGHT_RESOLVED，产出 `{role, seat, text, color}` 行，文案对齐 `render.py`）、`speechItems(events)`（发言 / 遗言 / 系统行 / GM 行的统一 feed，文案对齐 `render.py`，含 `seq` 供回放截断）、`roundSegments(events)`（ReplayBar 分段：每段 `{fromSeq, toSeq, kind: "night" | "day"}`）。

- [ ] **Step 4: 跑测试确认通过**：`npm run check`。

- [ ] **Step 5: Commit**

```bash
git add frontend
git commit -m "feat(frontend): Vite+React+TS 脚手架、Nocturne tokens、TS 同构 reducer 与金样对拍 (issue #26)"
```

---

### Task 6: 前端 API 客户端、WS hook、stores

**Files:**
- Create: `frontend/src/api/{rest.ts, ws.ts, tokens.ts, agents.ts, providers.ts}`、`frontend/src/store/{game.ts, agents.ts, providers.ts}`、`frontend/src/api/{rest.test.ts, ws.test.ts}`、`frontend/src/store/game.test.ts`

**Interfaces:**
- Produces（规格 §6）：`ApiError`、`createGame/startGame/getMeta/getReplay/getEvents`；`useLiveEvents({gameId, token, enabled})`；`useGameStore`（`load`、`appendEvents`、`setCursor`、`viewState`、`play/pause/setSpeed/stepForward/stepBack`、`connection/error`）；`parseHash()`；`useAgentLibrary`、`useProviders`。

- [ ] **Step 1: 写失败测试**

`store/game.test.ts`（用金样 `std_9_kill_side-3.json`）：`load(meta)` 后 `head.state_version===0`；`appendEvents(events.slice(0,60))` → `head` 等于 `states[59]`（normalize 后）；重复追加同一批不改变；追加 `seq` 跳号的批次 → `gap` 被记录且不应用；`setCursor(30)` → `viewState()` 等于 `states[29]`；`setCursor(null)` → `head`；检查点：追加全部后 `viewState()` 在 `cursor=137` 与 `reduceAll(initial, events.slice(0,137))` 一致；`play()` + 假计时（`vi.useFakeTimers`）推进到末尾自动 `playing=false`。

`api/ws.test.ts`（假 `WebSocket` 类挂到 `globalThis`，记录 URL、可 `emit(frame)`、`close(code)`）：hook 连接 URL 含 `token` 与 `from_seq=1`；连续 emit 3 个 `game_event` 后一帧 `requestAnimationFrame`（用 `vi.useFakeTimers` + `vi.stubGlobal("requestAnimationFrame", cb=>setTimeout(cb,16))`）内只调用一次 `appendEvents`；`close(4404)` → `error` 为「对局不存在」且不重连；意外 `close(1006)` 且未终局 → 1s 后重连且 URL `from_seq=lastSeq+1`；收到 `game_over` 后 close 不重连。

`api/rest.test.ts`（假 `fetch`）：`createGame` 发 `POST /api/v1/games` 带 JSON；非 2xx → `ApiError{status, detail}`（detail 取响应 `detail`）；`getReplay` 带 `Authorization: Bearer`。

- [ ] **Step 2: 实现**

`api/rest.ts`：`async function req<T>(method, path, {token, body}) : Promise<T>`；`ApiError extends Error {status; detail}`。`api/tokens.ts`：`parseHash(): {route: "lobby"|"agents"|"providers"|"game", gameId?, token?, viewer?: "GM"|"SPECTATOR"}`（`#/g/{id}?gm=…` / `?spec=…`）。`store/game.ts`：按规格 §6；检查点每 50 条（`CHECKPOINT_EVERY = 50`）；`viewState()` memo 上一次 `(cursor, eventsLength)`。`api/ws.ts`：按规格 §6（`useRef` socket、缓冲 + rAF 刷入、退避 `[1000, 2000, 4000, 8000]`、关闭码文案、`game_over` 后不重连）；导出 `CLOSE_TEXT` 表。`store/agents.ts` / `store/providers.ts`：`{items, loading, error, refresh(), create(), update(), remove()}` + providers 的 `test(id, model?)`、`listModels(id)`；`api/agents.ts`、`api/providers.ts` 薄封装。

- [ ] **Step 3: `npm run check` 全绿；Commit**

```bash
git add frontend/src/api frontend/src/store
git commit -m "feat(frontend): REST/WS 客户端、事件日志 store（增量 reduce + 回放检查点）、档案库与 Provider store (issue #26)"
```

---

### Task 7: GamePage——组件、页面、错误态、回放

**Files:**
- Create: `frontend/src/components/{PhaseBar, SeatCircle, SpeechFeed, NightOverlay, NightSummary, PlayerStatusPanel, VotePanel, ElectionPanel, ReplayBar, ErrorState}/*.tsx` + `*.module.css`、`frontend/src/pages/GamePage.tsx`、`frontend/src/components/*.test.tsx`（3 个冒烟）
- Modify: `frontend/src/App.tsx`（hash 路由）

**视觉真值：** mockup `1c`（GM 夜晚）、`1d`（GM 白天）、`1g`（终局回放）、`1h`（观众）、`1e`（VotePanel）、`1f`（ElectionPanel）、`1i`（错误态）、`1j`（<900px）。把对应 HTML 的结构与内联样式转成 JSX + CSS Modules（颜色 / 尺寸改用 tokens 变量；`tint(c,p)` 用 `color-mix`）。

- [ ] **Step 1: 冒烟测试（先写）**：`SeatCircle.test.tsx`：给 9 座 state（用金样末态），`viewer="GM"` 渲染出角色缩写「狼」并有 3 个；`viewer="SPECTATOR"` 渲染 `?`；出局座位 `aria-label` 含「出局」；当前发言座位有 `data-speaking`。`SpeechFeed.test.tsx`：给 `speechItems` 产物，渲染发言卡与 `[GM]` 行；`cursor` 截断后条数减少。`ReplayBar.test.tsx`：拖动 range 触发 `setCursor`；「回到直播」在 `cursor=null` 时禁用。

- [ ] **Step 2: 实现组件**（每个组件的 props 只来自 store 选择器与 `GameState`/`Event[]`；无内部过滤）：
  - `PhaseBar({state, mode, viewer, connection, lastSeq, totalSeq, reconnectHint})`。
  - `SeatCircle({state, viewer, speaking, votes, nightLines})`：座位坐标 `50 + 41cos/sin(-90° + i·360/n)`，SVG 连线层（仅 GM 夜间：从 `nightSummary` 派生 `{from, to, color, dashed}`）。
  - `SpeechFeed({items, cursor, gmRows})`：自动滚动 + 「↓ 有 N 条新消息」+ 回放截断提示。
  - `NightOverlay({phase, viewer})`：仅夜间阶段渲染。
  - 右栏：`NightSummary`（GM）/ 观众占位；`PlayerStatusPanel`（发言顺序、警徽流、GM 备注行仅 GM）；`VotePanel`（投票 / PK 两态）；`ElectionPanel`（子阶段进度、上警名单、警下计票、当选 / 方向 / 流失）。`GamePage` 按 `state.phase`/`election_stage` 选择右栏。
  - `ReplayBar({cursor, total, playing, speed, segments, onCursor, onPlay, onPause, onSpeed, onStep, onLive, live})` + 键盘（空格 / ← / →）。
  - `ErrorState({kind: "4404"|"4409"|"auth"|"reconnecting", attempt})`。
  - `GamePage`：解析 hash → `getMeta`（403 → 未开局轮询 2s / 404）→ `useGameStore.load(meta)` → 若已终局 `getReplay` 装入 `mode="replay"`，否则 `useLiveEvents`；布局 grid（`48px [40px] minmax(0,1fr) 52px` / 三栏 `320px minmax(0,1fr) 320px`；`@media (max-width: 900px)` 纵向堆叠）。
  - `App.tsx`：hash 路由 + `.nav`（新的一局 / Agent 档案库 / 模型服务）；`#/g/…` 页不显示 nav（GamePage 有自己的顶栏）。

- [ ] **Step 3: 手工验收**：`make serve`（后端）+ `make fe-dev`；用 `curl` 建一局零 LLM 全随机 bot（`POST /api/v1/games` + `/start`），打开 `http://localhost:5173/#/g/{id}?gm=…` 观看到终局并拖回放；`?spec=` 链接对照。把观察到的问题修完再提交。

- [ ] **Step 4: `npm run check` 全绿；Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): GamePage——座位环 / 发言流 / 夜间遮罩与连线 / 投票竞选面板 / 回放条 / 错误态 (issue #26)"
```

---

### Task 8: Lobby 三步、AgentLibrary / AgentEditor、Providers / ProviderEditor

**Files:**
- Create: `frontend/src/pages/{Lobby, AgentLibrary, Providers}.tsx`、`frontend/src/components/{PresetCard, AgentCard, AgentEditor, PersonalityEditor, SkillPicker, ModelSelect, SeatAssignment, ProviderCard, ProviderEditor, ShareDialog, Drawer, ConfirmDialog}/*.tsx` + css、`frontend/src/lib/{seats.ts, personality.ts, memoryId.ts}`、测试 `seats.test.ts`、`personality.test.ts`、`SeatAssignment.test.tsx`、`AgentEditor.test.tsx`、`ProviderEditor.test.tsx`

**视觉真值：** mockup `2a`（三步 · 步骤 2）、`2b`（档案库）、`2c`（AgentEditor 抽屉）、`2d`（空态 / 删除确认 / preset 卡片）、`3a`（Providers）、`3b`（ProviderEditor）、`3c`（ModelSelect 三态）、`3d`（Provider 辅助态）、`1b`（分享卡片）。

- [ ] **Step 1: 纯函数与测试先行**
  - `lib/seats.ts`：`buildAgentsPayload(assignment: {seat: number, agentId: string | null}[], fill: string | null, library) -> Record<string, AgentProfile>`（座位专属 → `"{seat}"`；`fill` → `"*"`；随机 bot 不写；档案对象去掉 `name` 以外的库字段，只发 `AgentProfile` 形状）；`memoryConflicts(assignment, library) -> Map<agentId, seat>`（同 `memory_id` 二次选择禁用）。测试覆盖 mockup 2a 的示例分配（0 夜枭 / 1 铁齿 / 3 狐语 / 4、5 老好人 / 8 沉默者 / 其余 * 老好人）。
  - `lib/personality.ts`：`renderPersonalityPreview(spec)`（只读预览，按后端 `render_personality` 顺序：描述 → 特质按强度降序 → 预设隐式句 → 说话风格 → 末句；词表与文案从 `backend/app/agent/personality.py` 抄录）；`FORBIDDEN_PHRASES` 复制用于即时提示；`toPersonalitySpec(form) -> PersonalitySpec | null`（全空 → null，不提交）。测试：描述含「无视规则」→ 命中提示；空表单 → null；MBTI 四轴 → `{system:"MBTI", value:"INTJ"}`。
  - `lib/memoryId.ts`：`suggestMemoryId(name)`（拉丁 / 数字保留，中文转 `agent-` + 简单哈希后 6 位十六进制，长度 ≤ 64，只含 `[A-Za-z0-9_-]`）。

- [ ] **Step 2: 组件与页面**（结构照 mockup）：
  - `Lobby`：左列步骤 + 汇总卡；步骤 1 `PresetCard` 列表（`GET /presets`）+ seed；步骤 2 `SeatAssignment`（档案库侧栏 `AgentCard` 精简 + 座位表下拉 + 工具行三按钮 + 冲突提示）；步骤 3 汇总 → `createGame({preset, config_override:{seed?}, agents})` → `startGame` → `ShareDialog`（GM / 观众链接 + 复制 + 警示）→ 「进入上帝视角」跳 `#/g/…?gm=`。错误 `detail` 原文展示。
  - `AgentLibrary`：网格 `AgentCard` + 新建 / 导入 JSON（文件选择 → `POST` 逐个，失败列出）/ 导出全部（下载 JSON——用 `Blob` + `<a download>`）；编辑 / 复制（名字加「 副本」）/ 删除（`ConfirmDialog`）；空态。
  - `AgentEditor`（`Drawer` 620px）：四组（基本含 `ModelSelect`：Provider 下拉（`useProviders`）→ 模型下拉（`listModels`，可手输）；兼容模式项；无 provider 引导）；`SkillPicker`（`GET /skills`，「全部（*）」开关）；`PersonalityEditor`（描述 / 特质 chips + 滑块 / 预设 seg + MBTI 四组 / Big Five 5 滑块 / 说话风格 / 预览卡）；记忆（`memory_id` + 按名字生成 + 唯一提示）；底部护栏提示 + 保存 / 取消。
  - `Providers`：`ProviderCard` 网格（引用数由 `useAgentLibrary` 计算）+ `ProviderEditor`（`Drawer` 560px：名字 / 类型 chips（选后填默认地址）/ 地址 / 密钥（留空不改 / 清除）/ 默认模型 + 「↻ 拉取模型列表」下拉面板 / 「测试连接」结果条）；删除被引用 → 409 `detail` 弹 `ConfirmDialog` 只读版；空态。

- [ ] **Step 3: 组件测试**：`SeatAssignment.test.tsx`（选择档案后 payload 正确；同 memory_id 第二处禁用）；`AgentEditor.test.tsx`（名字必填、描述超 300 计数红、空人格不提交、保存调用 `createAgent` 且 body 形状为 `AgentProfile`）；`ProviderEditor.test.tsx`（类型 Ollama 自动填地址；密钥留空不改 → body 无 `api_key`；「清除」→ `api_key: ""`）。

- [ ] **Step 4: 手工验收**：新建 Provider（本地 Ollama，测试连接 / 拉模型）、新建两个 Agent（不同人格 / 技能 / memory_id）、分配到座位并开局；无 Ollama 时用兼容模式 `ollama/…` 或全随机 bot 验证流程。

- [ ] **Step 5: `npm run check` 全绿；Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): 三步建局、Agent 档案库与编辑器（人格 / 技能 / 记忆 / ModelSelect）、模型服务页 (issue #26)"
```

---

### Task 9: 文档、PRD、冒烟、收尾

**Files:**
- Modify: `README.md`（「前端 / Frontend」小节：安装 / 开发 / 构建 / 金样更新流程 / 链接含 token 安全提示 / 模型服务与档案库；API 表加 `gm_token`、`/agents`、`/providers`、`/skills`、`/presets`）、`docs/specs/requirements.md`（§5.2 端点表；§7.4 帧字段修正与 `gm_token`；§4.4 补 Provider 一段）、`CLAUDE.md`（前端一行：`frontend/` 的 `npm run check`；TS reducer 金样约定）、`backend/tests/test_agent_bench.py`（env 门控：真实模型 + provider 走一局，验证凭据注入路径）

- [ ] **Step 1**：`test_agent_bench.py` 追加 `test_provider_bound_client_smoke`（`AGENTHOWL_SMOKE_MODEL` 下，用 `Provider(kind="ollama", api_base=env 或默认)` 构造 `build_agent_port(..., provider=p)` 的端口跑 1 局，断言终局）。
- [ ] **Step 2**：文档；`make check`（含 `fe-check`）全绿；`make fe-build` 后 `make serve` 打开 `http://localhost:8000/` 能看到前端（静态挂载）。
- [ ] **Step 3: Commit**

```bash
git add README.md docs/specs/requirements.md CLAUDE.md backend/tests/test_agent_bench.py
git commit -m "docs: M3 前端、GM token、档案库、模型服务说明；PRD 帧字段修正；provider 冒烟 (issue #26)"
```

---

## Self-Review

- **Spec 覆盖**：§1 判据 → T1（GM）、T4（金样 / 静态）、T5（工程 / 金样测试）、T7（直播 / 回放 / 零过滤）、T2（档案库）、T3（Provider）；§2 → T1；§2b → T2；§2c → T3（凭据绑定端口客户端为实现期简化，规格附注）；§3 → T4；§4 → T5；§5 → T5；§6 → T6；§7（含 §7.5 定稿）→ T7 / T8；§8 → T4 / T9；§9 → 各任务测试；§10 不在范围无任务。
- **占位符扫描**：前端组件步骤以 mockup `id` 为结构真值、以 props 契约与测试为行为真值——不是 TBD。后端任务代码完整。
- **类型一致性**：`resolve_model(model, provider)`、`to_agent_config(profile, game_config, provider=None)`、`build_agent_port(..., provider=None)`、`GameRegistry(provider_store=)`、`GameHandle.providers`、`create_app(agents_dir, agent_library, providers_dir, provider_store, provider_probe, frontend_dist)`、`ProviderProbe.test/list_models`、`export_fixture(preset, seed)`；前端 `initialState/reduce/reduceAll/normalizeState`、`useGameStore` 动作、`useLiveEvents` 参数、`buildAgentsPayload` 在任务间一致。
- **import 方向**：`app/agent/provider.py` 纯 pydantic；`provider_probe.py` 在方法内 import litellm / httpx；`registry`/`api`/`cli` 不触碰 `agent_player` 模块级；`export_fixture` 在 cli 下只依赖 engine / store / bot。
