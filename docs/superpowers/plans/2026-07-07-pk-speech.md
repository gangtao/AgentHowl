# PK 发言轮 实施计划（Implementation Plan）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 issue #5：平票者先在 PK 台按座号升序发言，再由未平票者重投——覆盖放逐 `VOTE_PK` 与竞选 `SHERIFF_PK` 两处。

**Architecture:** 复用发言队列机制，零新状态、零新事件：入 PK 的 `PHASE_CHANGED` 携带 `speech_order=tie`（既有 reduce 置队列并复位 `speech_idx`）；`expected_actors` 对两个 PK 阶段改为「发言先行」（队列未耗尽→当前发言者；耗尽→既有投票人集合）；`Speak` 校验门在 PK 发言期放行；PK 发言即 `PLAYER_SPOKE`（其 reduce 已推进 `speech_idx`）。Bot 加一个 PK 发言分支。

**Tech Stack:** 既有 M1 引擎（Python 3.11 + Pydantic v2）。命令在 `backend/` 下运行。

## Global Constraints

- 发言未完时投票在结构上不可开放（expected_actors 发言先行）；队列耗尽后 `Speak` → `WRONG_PHASE`（不重开越权口子）。
- PK 发言顺序 = `tie` 升序（`count_votes` 已返回升序 tuple），不可配置。
- 零新事件/零新状态字段；`reduce==live` 经既有事件结构性成立。
- 引擎零 IO；注释中文/标识符英文；`uv run mypy app`（strict）+ `uv run ruff check .` + `uv run ruff format --check .` 全程干净。
- 全量回归（含确定性、500 局扫描）必须通过——bot 将真实走 PK 发言路径。
- 提交信息以 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 结尾。

## File Structure

| 文件 | 改动 |
|---|---|
| `backend/app/engine/phases.py` | `expected_actors` 的 `VOTE_PK`/`SHERIFF_PK` 发言先行分支 |
| `backend/app/engine/engine.py` | `Speak` 校验门扩展；两处入 PK 的 `PHASE_CHANGED` 携带 `speech_order` |
| `backend/app/cli/bot.py` | PK 发言分支 |
| `backend/tests/test_pk_speech.py` | 新测试文件 |

---

### Task 1: 发言先行 —— expected_actors + Speak 校验门

**Files:**
- Modify: `backend/app/engine/phases.py`（`expected_actors` 的 `VOTE_PK` 分支约 102 行、`SHERIFF_PK` 分支）
- Modify: `backend/app/engine/engine.py`（`_validate` 的 Speak 门约 199 行）
- Test: `backend/tests/test_pk_speech.py`（新建）

**Interfaces:**
- Produces: `expected_actors(VOTE_PK/SHERIFF_PK)`——`speech_idx < len(speech_order)` 时返回 `{speech_order[speech_idx]}`，否则返回既有投票人集合；`Speak` 在 PK 发言期合法（队列耗尽后 `WRONG_PHASE`）。Task 2 依赖此语义。
- 行为兼容性：旧入口不带 `speech_order`（队列耗尽态）时，两分支立即落入投票人集合——Task 1 单独合入行为零变化。

- [ ] **Step 1: 写失败测试**

Create `backend/tests/test_pk_speech.py`：
```python
"""PK 发言轮（issue #5）：平票者先发言，未平票者后投票。"""

from app.engine.actions import DayVote, Speak
from app.engine.config import Faction, RoleType, build_preset
from app.engine.engine import step
from app.engine.events import EventType
from app.engine.phases import Phase, expected_actors
from app.engine.state import GameState, Player


def _players(n: int, wolves: tuple[int, ...] = (0,)) -> tuple[Player, ...]:
    return tuple(
        Player(
            seat=i,
            display_name=f"P{i}",
            role=RoleType.WEREWOLF if i in wolves else RoleType.VILLAGER,
            faction=Faction.WOLF if i in wolves else Faction.GOOD,
        )
        for i in range(n)
    )


def _state(n: int = 4, **kw: object) -> GameState:
    cfg = build_preset("std_9_kill_side").model_copy(update={"num_players": n, "seed": 1})
    base: dict[str, object] = {
        "game_id": "g",
        "config": cfg,
        "phase": Phase.VOTE_PK,
        "round": 2,
        "players": _players(n),
    }
    base.update(kw)
    return GameState(**base)  # type: ignore[arg-type]


def test_pk_expected_speaker_first_then_voters() -> None:
    # 队列未耗尽 -> 当前平票发言者；耗尽 -> 未平票投票人
    st = _state(vote_candidates=(2, 3), speech_order=(2, 3), speech_idx=0, tie_round=1)
    assert expected_actors(st) == {2}
    st2 = st.model_copy(update={"speech_idx": 1})
    assert expected_actors(st2) == {3}
    st3 = st.model_copy(update={"speech_idx": 2})
    assert expected_actors(st3) == {0, 1}


def test_sheriff_pk_expected_speaker_first() -> None:
    st = _state(
        n=5,
        phase=Phase.SHERIFF_PK,
        round=1,
        sheriff_candidates=(1, 2),
        speech_order=(1, 2),
        speech_idx=0,
    )
    assert expected_actors(st) == {1}
    st2 = st.model_copy(update={"speech_idx": 2})
    assert expected_actors(st2) == {0, 3, 4}


def test_pk_speak_validation() -> None:
    st = _state(vote_candidates=(2, 3), speech_order=(2, 3), speech_idx=0, tie_round=1)
    # 当前发言者可发言
    assert step(st, Speak(actor_seat=2, content="pk")).rejection is None
    # 非当前发言者 -> NOT_YOUR_TURN
    assert step(st, Speak(actor_seat=3, content="pk")).rejection is not None
    # 发言期投票 -> 拒（投票人不在 expected）
    assert step(st, DayVote(actor_seat=0, target_seat=2)).rejection is not None
    # 队列耗尽后 Speak -> 拒（WRONG_PHASE）
    st_done = st.model_copy(update={"speech_idx": 2})
    assert step(st_done, Speak(actor_seat=2, content="late")).rejection is not None
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_pk_speech.py -v`
Expected: FAIL（发言先行分支不存在：`expected_actors` 直接返回投票人；Speak 在 VOTE_PK 被拒）。

- [ ] **Step 3: 实现**

`backend/app/engine/phases.py` —— 把 `VOTE_PK` 分支整体替换为（`SHERIFF_PK` 同法；`SHERIFF_PK` 分支当前紧随 `SHERIFF_ELECTION` 块之后）：
```python
    if ph == Phase.VOTE_PK:
        # PK 发言先行：平票者按队列依次发言，耗尽后才轮到未平票投票人
        if state.speech_idx < len(state.speech_order):
            return {state.speech_order[state.speech_idx]}
        return {
            p.seat
            for p in living(state)
            if p.can_vote and p.seat not in state.vote_candidates and p.seat not in state.votes
        }
```
```python
    if ph == Phase.SHERIFF_PK:
        # PK 发言先行（同 VOTE_PK）
        if state.speech_idx < len(state.speech_order):
            return {state.speech_order[state.speech_idx]}
        return {
            p.seat
            for p in living(state)
            if p.can_vote
            and p.seat not in state.sheriff_candidates
            and p.seat not in state.sheriff_votes
        }
```

`backend/app/engine/engine.py` —— 把 `_validate` 的 Speak 门整体替换为：
```python
    if isinstance(action, Speak):
        # 发言合法阶段：白天发言、遗言，以及 PK 发言期（队列未耗尽）
        pk_speaking = state.phase in (Phase.VOTE_PK, Phase.SHERIFF_PK) and (
            state.speech_idx < len(state.speech_order)
        )
        if state.phase not in (Phase.DAY_SPEECH, Phase.LAST_WORDS) and not pk_speaking:
            return RejectedReason.WRONG_PHASE
        if (
            (state.phase == Phase.DAY_SPEECH or pk_speaking)
            and state.config.speech_order_rule == SpeechOrderRule.BIDDING
        ):
            return RejectedReason.BIDDING_NOT_IMPLEMENTED
        return None
```

- [ ] **Step 4: 运行确认通过 + 全量回归（行为零变化门）**

Run: `uv run pytest tests/test_pk_speech.py -v && uv run pytest -q`
Expected: 新测试 PASS（3 项）；全量 160 tests 全绿——旧入口不带 speech_order，PK 队列恒为耗尽态，Task 1 单独合入行为零变化（确定性字节级不变）。
Run: `uv run mypy app && uv run ruff check . && uv run ruff format --check .`
Expected: 干净。

- [ ] **Step 5: 提交**

```bash
git add backend/app/engine/phases.py backend/app/engine/engine.py backend/tests/test_pk_speech.py
git commit -m "feat(engine): PK 阶段发言先行的 expected_actors 与 Speak 校验门"
```

### Task 2: 入 PK 携带发言队列 + bot + 全流程

**Files:**
- Modify: `backend/app/engine/engine.py`（`_tally_and_continue` 平票分支约 1155 行；`_advance_election` PK 分支）
- Modify: `backend/app/cli/bot.py`（PK 发言分支）
- Test: `backend/tests/test_pk_speech.py`（追加）

**Interfaces:**
- Consumes: Task 1 的发言先行语义。
- Produces: 入 `VOTE_PK`/`SHERIFF_PK` 的 `PHASE_CHANGED` 携带 `speech_order=tie`；bot 在 PK 发言期返回 `Speak`。

- [ ] **Step 1: 写失败测试（两处全流程）**

Append 到 `backend/tests/test_pk_speech.py`：
```python
def test_exile_pk_full_flow() -> None:
    # 4 人：构造 2-2 平票 -> PK 发言（升序）-> 未平票者重投 -> 放逐
    st = _state(
        phase=Phase.VOTE,
        votes={0: 2, 1: 3, 2: 3},
        vote_candidates=(),
        tie_round=0,
    )
    res = step(st, DayVote(actor_seat=3, target_seat=2))  # 2-2：{2:2, 3:2}
    assert res.rejection is None
    st = res.state
    assert st.phase == Phase.VOTE_PK
    assert st.speech_order == (2, 3)  # 平票者按座号升序上 PK 台
    assert st.speech_idx == 0
    assert expected_actors(st) == {2}

    st = step(st, Speak(actor_seat=2, content="pk-2")).state
    assert expected_actors(st) == {3}
    st = step(st, Speak(actor_seat=3, content="pk-3")).state
    # 发言完毕 -> 仅未平票者投票
    assert expected_actors(st) == {0, 1}
    st = step(st, DayVote(actor_seat=0, target_seat=2)).state
    res = step(st, DayVote(actor_seat=1, target_seat=2))
    assert res.rejection is None
    # 2 被放逐（事件流含 PLAYER_EXILED(seat=2)）
    exiled = [
        e
        for e in res.events
        if e.type == EventType.PLAYER_EXILED and getattr(e.payload, "seat", None) == 2
    ]
    assert exiled


def test_sheriff_pk_full_flow() -> None:
    from app.engine.actions import SheriffAction, SheriffActionType
    from app.engine.config import SpeechOrderRule

    # 5 人竞选：候选 (1,2)，警下 {0,3,4}；0 投 1、3 投 2，4 号最后投 2 -> 2-2 平票入 PK
    cfg = build_preset("std_9_kill_side").model_copy(
        update={
            "num_players": 5,
            "seed": 1,
            "speech_order_rule": SpeechOrderRule.FIXED_CLOCKWISE,  # 绕开当选后的方向决策子阶段
        }
    )
    st = GameState(
        game_id="g",
        config=cfg,
        phase=Phase.SHERIFF_ELECTION,
        round=1,
        players=_players(5, wolves=(0, 1, 2)),  # 3 狼 2 民，避免结算即终局
        election_stage="vote",
        sheriff_candidates=(1, 2),
        sheriff_votes={0: 1, 3: 2},
        night_deaths=(),
        resolved_first_night=True,
    )
    res = step(
        st, SheriffAction(actor_seat=4, action_type=SheriffActionType.VOTE_SHERIFF, target_seat=2)
    )
    assert res.rejection is None
    stp = res.state
    assert stp.phase == Phase.SHERIFF_PK
    assert stp.speech_order == (1, 2)
    assert expected_actors(stp) == {1}

    stp = step(stp, Speak(actor_seat=1, content="pk")).state
    stp = step(stp, Speak(actor_seat=2, content="pk")).state
    # 警下重投（候选人不投）
    assert expected_actors(stp) == {0, 3, 4}
    stp = step(stp, SheriffAction(actor_seat=0, action_type=SheriffActionType.VOTE_SHERIFF, target_seat=1)).state
    stp = step(stp, SheriffAction(actor_seat=3, action_type=SheriffActionType.VOTE_SHERIFF, target_seat=1)).state
    res = step(
        stp, SheriffAction(actor_seat=4, action_type=SheriffActionType.VOTE_SHERIFF, target_seat=1)
    )
    assert res.rejection is None
    assert res.state.sheriff_seat == 1


def test_full_games_with_pk_still_terminate() -> None:
    from app.cli.bot import run_game

    for seed in (5, 21, 34):
        cfg = build_preset("std_12_yn_hunter_guard").model_copy(update={"seed": seed})
        final, _ = run_game(cfg, game_id=f"pk{seed}")
        assert final.phase == Phase.GAME_OVER
```


- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_pk_speech.py::test_exile_pk_full_flow -v`
Expected: FAIL（`st.speech_order == (2, 3)` 断言失败——入 PK 未携带发言队列）。

- [ ] **Step 3: 两处入口携带 speech_order**

`backend/app/engine/engine.py`：

(a) `_tally_and_continue` 平票分支，把
```python
        state, e = _emit(
            state, EventType.PHASE_CHANGED, PhaseChangedPayload(to=Phase.VOTE_PK), Visibility.PUBLIC
        )
```
改为
```python
        state, e = _emit(
            state,
            EventType.PHASE_CHANGED,
            PhaseChangedPayload(to=Phase.VOTE_PK, speech_order=tie),
            Visibility.PUBLIC,
        )
```
（注释「Stage 1 简化为直接重投，PK 发言在 Stage 3 补」同步更新为「平票者先发言（issue #5），耗尽后未平票者重投」。）

(b) `_advance_election` 的 PK 分支，把
```python
        state, e = _emit(
            state,
            EventType.PHASE_CHANGED,
            PhaseChangedPayload(to=Phase.SHERIFF_PK),
            Visibility.PUBLIC,
        )
```
改为
```python
        state, e = _emit(
            state,
            EventType.PHASE_CHANGED,
            PhaseChangedPayload(to=Phase.SHERIFF_PK, speech_order=tie),
            Visibility.PUBLIC,
        )
```

- [ ] **Step 4: bot PK 发言分支**

`backend/app/cli/bot.py` —— 在 `RandomBot.choose_action` 中、`if ph in (Phase.VOTE, Phase.VOTE_PK):` 投票分支**之前**、且在警长 `if ph in (Phase.SHERIFF_ELECTION, Phase.SHERIFF_PK):` 投票分支**之前**（即两者都能被短路的位置，建议紧跟 `pl = player_at(state, seat)` 之后）插入：
```python
        if ph in (Phase.VOTE_PK, Phase.SHERIFF_PK) and state.speech_idx < len(state.speech_order):
            # PK 发言期：轮到的平票者发言
            return Speak(actor_seat=seat, content="(bot-pk)")
```

- [ ] **Step 5: 运行确认通过 + 全量回归**

Run: `uv run pytest tests/test_pk_speech.py -v && uv run pytest -q`
Expected: 全绿（163 tests；确定性、500 局扫描——bot 现在真实发 PK 言）。若扫描某 seed 卡在 VOTE_PK/SHERIFF_PK，检查 bot 分支位置是否先于两个投票分支。
Run: `uv run mypy app && uv run ruff check . && uv run ruff format --check .`
Expected: 干净。

- [ ] **Step 6: 提交**

```bash
git add backend/app/engine/engine.py backend/app/cli/bot.py backend/tests/test_pk_speech.py
git commit -m "feat(engine): 入 PK 携带发言队列，平票者先发言后重投，闭环 issue #5"
```

---

## Self-Review

**Spec 覆盖**：§2 机制（入口携带队列 + 发言先行 + PLAYER_SPOKE 复用）→Task 1（expected/门）+ Task 2（入口）；§3 校验（含 BIDDING 镜像、耗尽后 WRONG_PHASE）→Task 1 Step 3；§4 bot→Task 2 Step 4；§5 交互（自爆/零投票人/确定性）→无代码改动需求，Task 2 Step 5 回归覆盖；§6 测试→两任务测试步骤；§7 范围外无任务——正确。

**占位扫描**：无 TBD/TODO；`test_sheriff_pk_full_flow` 已按干净版本写出（4 号直接投 2 制造 2-2；enum 传值而非裸字符串）。

**类型一致性**：`PhaseChangedPayload(to=..., speech_order=...)` 与既有 payload 字段一致；`expected_actors` 分支引用的 `vote_candidates`/`sheriff_candidates`/`sheriff_votes`/`votes` 均为既有字段；Speak 门中 `SpeechOrderRule` 已在 engine.py import。
