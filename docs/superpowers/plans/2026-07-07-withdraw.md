# 退水 实施计划（Implementation Plan）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 issue #6：candidacy 与 vote 之间新增 `withdraw` 确认子阶段，候选人可退水（`SHERIFF_WITHDREW` 事件，实现该预留类型），退水者失去整场竞选（含 PK）投票权，白天投票不受影响。

**Architecture:** 事实 `sheriff_withdrawn`（frozenset，经 `SHERIFF_WITHDREW` reduce 写入：出候选集 + 入退水集）；游标 `sheriff_confirmed`（确认进度，`model_copy`）。`_advance_election` 的 candidacy 完毕改进 `withdraw` 子阶段，全员确认后候选集空 → 警徽流失，否则进 `vote`。`expected_actors` 加 withdraw 分支并在竞选 vote/SHERIFF_PK 投票人集合排除退水者；`_validate_sheriff`/`_apply_sheriff` 按 stage 分派 RUN/WITHDRAW 双语义；bot 1/8 概率退水。

**Tech Stack:** 既有 M1 引擎（Python 3.11 + Pydantic v2）。命令在 `backend/` 下运行。

## Global Constraints

- 事实（sheriff_withdrawn/候选集变更）只经 `SHERIFF_WITHDREW` 事件；`sheriff_confirmed` 是文档化游标例外。
- 退水剥夺整场竞选投票权（首轮 + SHERIFF_PK 重投）；白天 `DayVote` 不受影响。
- `EVENT_PAYLOAD_TYPES` 加 `SHERIFF_WITHDREW` 条目；`test_fail_loud.py` 映射完整性 reserved 集合移除之（issue #4 tripwire 按设计触发）。
- 全量回归（含确定性、500 局扫描）必须通过——bot 将真实走 withdraw 子阶段。
- 引擎零 IO；注释中文/标识符英文；`uv run mypy app`（strict）+ `uv run ruff check .` + `uv run ruff format --check .` 全程干净。
- 提交信息以 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 结尾。

## File Structure

| 文件 | 改动 |
|---|---|
| `backend/app/engine/state.py` | +`sheriff_withdrawn`（事实）、+`sheriff_confirmed`（游标）、election_stage 注释更新 |
| `backend/app/engine/events.py` | +`SheriffWithdrewPayload`、reduce 分支、`EVENT_PAYLOAD_TYPES` 条目 |
| `backend/app/engine/engine.py` | `_advance_election` withdraw 子阶段；`_validate_sheriff`/`_apply_sheriff` 双语义；vote 分支排除退水者 |
| `backend/app/engine/phases.py` | `expected_actors`：withdraw 分支 + vote/SHERIFF_PK 排除退水者 |
| `backend/app/cli/bot.py` | withdraw 子阶段 1/8 概率退水 |
| `backend/tests/test_withdraw.py` | 新测试文件 |
| `backend/tests/test_fail_loud.py` | reserved 集合更新 |

---

### Task 1: 退水事实 —— 字段 + SHERIFF_WITHDREW 事件 + 映射

**Files:**
- Modify: `backend/app/engine/state.py`（81-87 行竞选字段区）
- Modify: `backend/app/engine/events.py`（payload、reduce、`EVENT_PAYLOAD_TYPES`）
- Modify: `backend/tests/test_fail_loud.py`（映射完整性 reserved 集合）
- Test: `backend/tests/test_withdraw.py`（新建）

**Interfaces:**
- Produces: `GameState.sheriff_withdrawn: frozenset[int] = frozenset()`；`GameState.sheriff_confirmed: frozenset[int] = frozenset()`；`EventType.SHERIFF_WITHDREW`（枚举已存在，本任务实现）+ `SheriffWithdrewPayload(seat: int)` + reduce（候选集移除 + 退水集加入）+ 映射条目。Task 2 依赖这些名字。

- [ ] **Step 1: 写失败测试**

Create `backend/tests/test_withdraw.py`：
```python
"""退水（issue #6）：SHERIFF_WITHDREW 事实、确认子阶段与投票权剥夺。"""

from app.engine.actions import SheriffAction, SheriffActionType
from app.engine.config import Faction, RoleType, build_preset
from app.engine.events import (
    Event,
    EventType,
    SheriffWithdrewPayload,
    Visibility,
    reduce,
)
from app.engine.phases import Phase, expected_actors
from app.engine.state import GameState, Player


def _players(n: int, wolves: tuple[int, ...] = (0,)) -> tuple[Player, ...]:
    return tuple(
        Player(
            seat=i,
            display_name=f"P{i}",
            role=RoleType.WEREWOLF if i in wolves else RoleType.VILLAGER,
            faction=Faction.WOLF if i in wolves else Faction.GOOD,
        )
        for i in range(n)
    )


def _state(n: int = 6, **kw: object) -> GameState:
    cfg = build_preset("std_9_kill_side").model_copy(update={"num_players": n, "seed": 1})
    base: dict[str, object] = {
        "game_id": "g",
        "config": cfg,
        "phase": Phase.SHERIFF_ELECTION,
        "round": 1,
        "players": _players(n, wolves=(0, 1)),
        "night_deaths": (),
        "resolved_first_night": True,
    }
    base.update(kw)
    return GameState(**base)  # type: ignore[arg-type]


def test_withdrew_event_reduces_out_of_candidates_into_withdrawn() -> None:
    st = _state(sheriff_candidates=(1, 2, 3), election_stage="withdraw")
    ev = Event(
        seq=1,
        game_id="g",
        ts=1.0,
        type=EventType.SHERIFF_WITHDREW,
        actor_seat=2,
        payload=SheriffWithdrewPayload(seat=2),
        visibility=Visibility.PUBLIC,
    )
    new = reduce(st, ev)
    assert new.sheriff_candidates == (1, 3)
    assert new.sheriff_withdrawn == frozenset({2})
    assert st.sheriff_candidates == (1, 2, 3)  # 原状态不变
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_withdraw.py -v`
Expected: FAIL（`ImportError: cannot import name 'SheriffWithdrewPayload'`）。

- [ ] **Step 3: 实现**

`backend/app/engine/state.py` —— 竞选字段区（`sheriff_votes` 之后）追加两字段，并把 `election_stage` 注释更新为含 `"withdraw"`：
```python
    sheriff_withdrawn: frozenset[int] = frozenset()  # 退水者（事实：整场竞选失去投票权）
    sheriff_confirmed: frozenset[int] = frozenset()  # withdraw 子阶段确认进度（游标）
```
`election_stage` 注释改为：`# ""/"candidacy"/"withdraw"/"vote"/"direction"/"announce"（PK 由 phase==SHERIFF_PK 区分）`

`backend/app/engine/events.py`：

(a) payload（`SheriffElectedPayload` 附近）：
```python
class SheriffWithdrewPayload(EventPayload):
    seat: int
```

(b) reduce 分支（SHERIFF_CANDIDACY 分支之后）：
```python
    if t == EventType.SHERIFF_WITHDREW and isinstance(p, SheriffWithdrewPayload):
        candidates = tuple(s for s in state.sheriff_candidates if s != p.seat)
        return {
            "sheriff_candidates": candidates,
            "sheriff_withdrawn": state.sheriff_withdrawn | {p.seat},
        }
```

(c) `EVENT_PAYLOAD_TYPES` 加条目（SHERIFF_ELECTED 附近）：
```python
    EventType.SHERIFF_WITHDREW: SheriffWithdrewPayload,
```

`backend/tests/test_fail_loud.py` —— `test_mapping_covers_exactly_implemented_types` 的 `reserved` 集合移除 `EventType.SHERIFF_WITHDREW`（剩 GAME_CREATED/GAME_STARTED/SHERIFF_BADGE_LOST 三项）。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `uv run pytest tests/test_withdraw.py tests/test_fail_loud.py -v && uv run pytest -q`
Expected: 全绿（164 tests；行为零变化——事件尚无发射方）。
Run: `uv run mypy app && uv run ruff check . && uv run ruff format --check .`
Expected: 干净。

- [ ] **Step 5: 提交**

```bash
git add backend/app/engine/state.py backend/app/engine/events.py backend/tests/test_withdraw.py backend/tests/test_fail_loud.py
git commit -m "feat(engine): SHERIFF_WITHDREW 事件与 sheriff_withdrawn 事实（实现预留类型）"
```

### Task 2: withdraw 子阶段流程 + 投票权剥夺 + bot

**Files:**
- Modify: `backend/app/engine/engine.py`（`_advance_election`、`_validate_sheriff`、`_apply_sheriff`）
- Modify: `backend/app/engine/phases.py`（`expected_actors`）
- Modify: `backend/app/cli/bot.py`
- Test: `backend/tests/test_withdraw.py`（追加）

**Interfaces:**
- Consumes: Task 1 的字段/事件/映射。
- Produces: `election_stage` 流转 `candidacy → withdraw → vote`；RUN/WITHDRAW 在 withdraw 子阶段的双语义；投票人集合排除 `sheriff_withdrawn`。

- [ ] **Step 1: 写失败测试**

Append 到 `backend/tests/test_withdraw.py`：
```python
def test_withdraw_stage_flow_and_vote_forfeiture() -> None:
    from app.engine.engine import step

    # 6 人，候选 (1,2,3)，withdraw 子阶段：2 退水，1/3 坚持
    st = _state(
        sheriff_candidates=(1, 2, 3),
        sheriff_declared=frozenset({0, 1, 2, 3, 4, 5}),
        election_stage="withdraw",
    )
    assert expected_actors(st) == {1, 2, 3}

    st = step(
        st, SheriffAction(actor_seat=1, action_type=SheriffActionType.RUN_FOR_SHERIFF)
    ).state
    assert expected_actors(st) == {2, 3}
    res = step(st, SheriffAction(actor_seat=2, action_type=SheriffActionType.WITHDRAW))
    assert res.rejection is None
    assert any(e.type == EventType.SHERIFF_WITHDREW for e in res.events)
    st = res.state
    assert st.sheriff_withdrawn == frozenset({2})
    assert 2 not in st.sheriff_candidates
    assert expected_actors(st) == {3}
    st = step(
        st, SheriffAction(actor_seat=3, action_type=SheriffActionType.RUN_FOR_SHERIFF)
    ).state

    # 全员确认 -> vote 阶段；退水者 2 不在投票人集合（警下 = {0,4,5}）
    assert st.election_stage == "vote"
    assert expected_actors(st) == {0, 4, 5}
    # 退水者显式投票被拒（CANNOT_VOTE）
    from app.engine.actions import RejectedReason

    r = step(st, SheriffAction(actor_seat=2, action_type=SheriffActionType.VOTE_SHERIFF, target_seat=1))
    assert r.rejection == RejectedReason.CANNOT_VOTE


def test_all_withdraw_badge_lost() -> None:
    from app.engine.engine import step

    st = _state(sheriff_candidates=(1, 2), election_stage="withdraw")
    st = step(st, SheriffAction(actor_seat=1, action_type=SheriffActionType.WITHDRAW)).state
    res = step(st, SheriffAction(actor_seat=2, action_type=SheriffActionType.WITHDRAW))
    assert res.rejection is None
    # 全员退水 -> 警徽流失并续接（离开竞选阶段）
    elected = [e for e in res.events if e.type == EventType.SHERIFF_ELECTED]
    assert len(elected) == 1 and elected[0].payload.seat is None  # type: ignore[attr-defined]
    assert res.state.sheriff_seat is None


def test_withdrawn_keeps_day_vote() -> None:
    from app.engine.actions import DayVote
    from app.engine.engine import step

    # 退水者在白天 VOTE 阶段仍可投票
    st = _state(
        phase=Phase.VOTE,
        round=1,
        sheriff_withdrawn=frozenset({2}),
        votes={},
        vote_candidates=(),
    )
    r = step(st, DayVote(actor_seat=2, target_seat=0))
    assert r.rejection is None


def test_full_games_with_withdraw_terminate() -> None:
    from app.cli.bot import run_game

    for seed in (2, 9, 27):
        cfg = build_preset("std_12_yn_hunter_guard").model_copy(update={"seed": seed})
        final, _ = run_game(cfg, game_id=f"wd{seed}")
        assert final.phase == Phase.GAME_OVER
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_withdraw.py::test_withdraw_stage_flow_and_vote_forfeiture -v`
Expected: FAIL（withdraw 子阶段不存在，`expected_actors` 返回空集）。

- [ ] **Step 3: 实现流程**

`backend/app/engine/phases.py` —— `expected_actors` 的 SHERIFF_ELECTION 块加 withdraw 分支（放在 `"candidacy"` 分支之后），并在竞选 `"vote"` 分支与 `SHERIFF_PK` 投票人集合各加排除条件：
```python
        if state.election_stage == "withdraw":
            # 退水确认：尚未表态的候选人
            return {s for s in state.sheriff_candidates if s not in state.sheriff_confirmed}
```
`"vote"` 分支与 `SHERIFF_PK` 的集合推导式各追加一行条件：
```python
                and p.seat not in state.sheriff_withdrawn
```

`backend/app/engine/engine.py`：

(a) `_advance_election` 的 candidacy 分支改为进 withdraw，并新增 withdraw 收尾（放在 candidacy 分支之后）：
```python
    if state.election_stage == "candidacy":
        # 全员声明完毕
        if not state.sheriff_candidates:
            return _finish_election(state, elected=None, events=events)
        # 进入退水确认子阶段
        state = state.model_copy(update={"election_stage": "withdraw", "sheriff_confirmed": frozenset()})
        return state, events
    if state.election_stage == "withdraw":
        # 全员确认完毕：全退水 -> 警徽流失；否则进入警下投票
        if not state.sheriff_candidates:
            return _finish_election(state, elected=None, events=events)
        state = state.model_copy(update={"election_stage": "vote", "sheriff_votes": {}})
        return state, events
```

(b) `_validate_sheriff` —— candidacy 分支之后插入 withdraw 分支；vote 分支加退水者拒绝：
```python
    if state.phase == Phase.SHERIFF_ELECTION and state.election_stage == "withdraw":
        if at not in (SheriffActionType.RUN_FOR_SHERIFF, SheriffActionType.WITHDRAW):
            return RejectedReason.WRONG_PHASE
        if a.actor_seat not in state.sheriff_candidates:
            return RejectedReason.NOT_A_CANDIDATE
        return None
```
vote 分支（`at != VOTE_SHERIFF` 检查之后）插入：
```python
        if a.actor_seat in state.sheriff_withdrawn:
            return RejectedReason.CANNOT_VOTE
```

(c) `_apply_sheriff` —— RUN/WITHDRAW 分支按 stage 分派（整体替换现有该分支）：
```python
    if at in (SheriffActionType.RUN_FOR_SHERIFF, SheriffActionType.WITHDRAW):
        if state.election_stage == "withdraw":
            # 退水确认子阶段：WITHDRAW=退水（事件写事实），RUN=坚持竞选（公开再确认）
            if at == SheriffActionType.WITHDRAW:
                s, e = _emit(
                    state,
                    EventType.SHERIFF_WITHDREW,
                    SheriffWithdrewPayload(seat=a.actor_seat),
                    Visibility.PUBLIC,
                    actor=a.actor_seat,
                )
            else:
                s, e = _emit(
                    state,
                    EventType.SHERIFF_CANDIDACY,
                    SheriffCandidacyPayload(seat=a.actor_seat, running=True),
                    Visibility.PUBLIC,
                    actor=a.actor_seat,
                )
            s = s.model_copy(update={"sheriff_confirmed": s.sheriff_confirmed | {a.actor_seat}})
            return s, [e]
        running = at == SheriffActionType.RUN_FOR_SHERIFF
        s, e = _emit(
            state,
            EventType.SHERIFF_CANDIDACY,
            SheriffCandidacyPayload(seat=a.actor_seat, running=running),
            Visibility.PUBLIC,
            actor=a.actor_seat,
        )
        return s, [e]
```
（engine.py 顶部 import 补 `SheriffWithdrewPayload`。）

- [ ] **Step 4: bot withdraw 分支**

`backend/app/cli/bot.py` —— 在 `election_stage == "candidacy"` 分支**之前**插入：
```python
        if ph == Phase.SHERIFF_ELECTION and state.election_stage == "withdraw":
            from app.engine.actions import SheriffAction, SheriffActionType

            quit_race = (
                rng.derive_int(
                    seed=seed, purpose=f"bot:{seat}:withdraw", seq=state.state_version, modulo=8
                )
                == 0
            )
            return SheriffAction(
                actor_seat=seat,
                action_type=(
                    SheriffActionType.WITHDRAW if quit_race else SheriffActionType.RUN_FOR_SHERIFF
                ),
            )
```

- [ ] **Step 5: 运行确认通过 + 全量回归**

Run: `uv run pytest tests/test_withdraw.py -v && uv run pytest -q`
Expected: 全绿（168 tests；确定性、500 局扫描——bot 现在真实走 withdraw 子阶段，偶发全员退水）。若扫描卡在 SHERIFF_ELECTION/withdraw，检查 bot 分支位置与 stage 字符串拼写。
Run: `uv run mypy app && uv run ruff check . && uv run ruff format --check .`
Expected: 干净。

- [ ] **Step 6: 提交**

```bash
git add backend/app/engine backend/app/cli/bot.py backend/tests/test_withdraw.py
git commit -m "feat(engine): 退水确认子阶段与整场竞选投票权剥夺，闭环 issue #6"
```

---

## Self-Review

**Spec 覆盖**：§2 子阶段流转→Task 2(a)；§3 数据模型（事实/事件/映射/游标）→Task 1；§4 投票权剥夺（expected + 显式拒绝 + 白天不受影响）→Task 2 phases/validate + `test_withdrawn_keeps_day_vote`；§5 交互→无代码需求，回归覆盖；§6 bot→Task 2 Step 4；§7 测试→两任务测试步骤 + `test_fail_loud` 更新；§8 范围外无任务——正确。

**占位扫描**：无 TBD/TODO；每步完整代码与期望输出。

**类型一致性**：`SheriffWithdrewPayload(seat)` 三处（events/engine/tests）一致；`sheriff_withdrawn`/`sheriff_confirmed` 字段名全程一致；`election_stage` 新值 `"withdraw"` 在 phases/engine/bot/tests 拼写一致；`CANNOT_VOTE` 为 actions.py 既有成员。
