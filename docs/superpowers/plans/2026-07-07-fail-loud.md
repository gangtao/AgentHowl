# 引擎 fail-loud 实施计划（Implementation Plan）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 issue #4：`reduce` 拒绝畸形/预留事件（`EngineInvariantError`），`_apply_sheriff` 兜底显式抛错——消除两处静默继续。

**Architecture:** `events.py` 新增模块级 `EVENT_PAYLOAD_TYPES` 映射（28 个已实现类型 → payload 类；4 个预留类型有意缺席），`reduce()` 顶部前置校验；`EngineInvariantError` 从 `engine.py` 移到 `events.py`（避免循环 import），`engine.py` re-export。`_apply_sheriff` 末尾隐式 vote_sheriff 兜底改显式守卫。合法事件路径零变化——确定性字节级不变是核心回归门。

**Tech Stack:** 既有 M1 引擎（Python 3.11 + Pydantic v2）。命令在 `backend/` 下运行。

## Global Constraints

- 合法事件路径行为零变化：`test_determinism` 字节级原样通过。
- 「未映射 = 未实现」：`GAME_CREATED`/`GAME_STARTED`/`SHERIFF_WITHDREW`/`SHERIFF_BADGE_LOST` 不入映射，被 reduce 到即抛错。
- `EngineInvariantError` 名字必须仍可 `from app.engine.engine import`（re-export，既有调用方零改动）。
- 引擎零 IO；注释中文/标识符英文；`uv run mypy app`（strict）+ `uv run ruff check .` + `uv run ruff format --check .` 全程干净。
- 提交信息以 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 结尾。

## File Structure

| 文件 | 改动 |
|---|---|
| `backend/app/engine/events.py` | +`EngineInvariantError`（自 engine.py 迁入）、+`EVENT_PAYLOAD_TYPES` 映射、`reduce()` 前置校验 |
| `backend/app/engine/engine.py` | 删本地 `EngineInvariantError` 定义，改 import；`_apply_sheriff` 兜底守卫 |
| `backend/tests/test_fail_loud.py` | 新测试文件 |

---

### Task 1: reduce 前置校验 —— 映射 + 异常迁移

**Files:**
- Modify: `backend/app/engine/events.py`（`reduce` 约 222 行；payload 类区之后加映射）
- Modify: `backend/app/engine/engine.py`（85 行的类定义删除、import 调整）
- Test: `backend/tests/test_fail_loud.py`（新建）

**Interfaces:**
- Produces: `events.EngineInvariantError(RuntimeError)`（并经 `engine.py` re-export）；`events.EVENT_PAYLOAD_TYPES: dict[EventType, type[EventPayload]]`（28 项）；`reduce()` 对未映射类型/payload 不匹配抛 `EngineInvariantError`。

- [ ] **Step 1: 写失败测试**

Create `backend/tests/test_fail_loud.py`：
```python
"""引擎 fail-loud（issue #4）：畸形事件与预留事件在 reduce 处抛错。"""

import pytest

from app.engine.config import Faction, RoleType, build_preset
from app.engine.events import (
    EVENT_PAYLOAD_TYPES,
    DeathAnnouncedPayload,
    EngineInvariantError,
    Event,
    EventType,
    PhaseChangedPayload,
    Visibility,
    reduce,
)
from app.engine.phases import Phase
from app.engine.state import GameState, Player


def _state() -> GameState:
    players = tuple(
        Player(seat=i, display_name=f"P{i}", role=RoleType.VILLAGER, faction=Faction.GOOD)
        for i in range(4)
    )
    return GameState(
        game_id="g",
        config=build_preset("std_9_kill_side"),
        phase=Phase.DAY_SPEECH,
        round=1,
        players=players,
    )


def _evt(type_: EventType, payload: object) -> Event:
    return Event(
        seq=1,
        game_id="g",
        ts=1.0,
        type=type_,
        actor_seat=None,
        payload=payload,  # type: ignore[arg-type]
        visibility=Visibility.GM_ONLY,
    )


def test_mismatched_payload_raises() -> None:
    # 已知类型 + 错误 payload 类 -> 抛错（此前静默 bump state_version）
    ev = _evt(EventType.PHASE_CHANGED, DeathAnnouncedPayload(seats=(1,)))
    with pytest.raises(EngineInvariantError, match="payload"):
        reduce(_state(), ev)


def test_reserved_type_raises() -> None:
    # 预留（未实现）类型 -> 抛错（此前静默无操作）
    ev = _evt(EventType.GAME_CREATED, PhaseChangedPayload(to=Phase.DAY_SPEECH))
    with pytest.raises(EngineInvariantError, match="未实现"):
        reduce(_state(), ev)


def test_mapping_covers_exactly_implemented_types() -> None:
    reserved = {
        EventType.GAME_CREATED,
        EventType.GAME_STARTED,
        EventType.SHERIFF_WITHDREW,
        EventType.SHERIFF_BADGE_LOST,
    }
    # 枚举新增成员时此断言强制作者决策：入映射（实现）或入 reserved（预留）
    assert set(EVENT_PAYLOAD_TYPES) == set(EventType) - reserved


def test_valid_event_still_reduces() -> None:
    ev = _evt(EventType.PHASE_CHANGED, PhaseChangedPayload(to=Phase.VOTE))
    new = reduce(_state(), ev)
    assert new.phase == Phase.VOTE
    assert new.state_version == 1


def test_exception_reexported_from_engine() -> None:
    from app.engine.engine import EngineInvariantError as FromEngine

    assert FromEngine is EngineInvariantError
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_fail_loud.py -v`
Expected: FAIL（`ImportError: cannot import name 'EVENT_PAYLOAD_TYPES'` / `EngineInvariantError` 不在 events）。

- [ ] **Step 3: 迁移异常 + 加映射 + reduce 前置校验**

`backend/app/engine/events.py`：

(a) 在 `Visibility` 枚举之前（模块头部区域）加异常类（与 engine.py 现有定义逐字一致）：
```python
class EngineInvariantError(RuntimeError):
    """引擎进入了不可能状态；绝不静默继续。"""
```

(b) 在全部 payload 类定义之后、`Event` 类之前，加映射（28 项，`WITCH_SAVED`/`WITCH_POISONED` 共用 `WitchActedPayload`）：
```python
# EventType -> payload 类。预留类型（GAME_CREATED/GAME_STARTED/SHERIFF_WITHDREW/
# SHERIFF_BADGE_LOST）有意缺席：未映射 = 未实现，被 reduce 到即抛错（fail-loud）。
EVENT_PAYLOAD_TYPES: dict[EventType, type[EventPayload]] = {
    EventType.ROLES_ASSIGNED: RolesAssignedPayload,
    EventType.ROUND_STARTED: RoundStartedPayload,
    EventType.PHASE_CHANGED: PhaseChangedPayload,
    EventType.VOTE_STARTED: VoteStartedPayload,
    EventType.GUARD_PROTECTED: GuardProtectedPayload,
    EventType.WOLF_KILL_PROPOSED: WolfKillProposedPayload,
    EventType.WOLF_KILL_DECIDED: WolfKillDecidedPayload,
    EventType.WITCH_SAVED: WitchActedPayload,
    EventType.WITCH_POISONED: WitchActedPayload,
    EventType.WITCH_POTION_CONSUMED: WitchPotionConsumedPayload,
    EventType.SEER_CHECKED: SeerCheckedPayload,
    EventType.NIGHT_RESOLVED: NightResolvedPayload,
    EventType.DEATH_ANNOUNCED: DeathAnnouncedPayload,
    EventType.PLAYER_SPOKE: PlayerSpokePayload,
    EventType.VOTE_CAST: VoteCastPayload,
    EventType.VOTE_RESULT: VoteResultPayload,
    EventType.PLAYER_EXILED: PlayerExiledPayload,
    EventType.ROLE_SKIPPED: RoleSkippedPayload,
    EventType.GAME_OVER: GameOverPayload,
    EventType.LAST_WORDS: LastWordsPayload,
    EventType.HUNTER_SHOT: HunterShotPayload,
    EventType.IDIOT_REVEALED: IdiotRevealedPayload,
    EventType.SHERIFF_CANDIDACY: SheriffCandidacyPayload,
    EventType.SHERIFF_VOTE_CAST: SheriffVoteCastPayload,
    EventType.SHERIFF_ELECTED: SheriffElectedPayload,
    EventType.SHERIFF_DIRECTION_SET: SheriffDirectionSetPayload,
    EventType.WOLF_SELF_DESTRUCT: WolfSelfDestructPayload,
    EventType.BADGE_PASSED: BadgePassedPayload,
}
```

(c) `reduce()` 顶部前置校验：
```python
def reduce(state: GameState, event: Event) -> GameState:
    """把单个事件应用到状态，返回新状态。唯一写路径。"""
    expected = EVENT_PAYLOAD_TYPES.get(event.type)
    if expected is None:
        raise EngineInvariantError(f"未实现的事件类型：{event.type}")
    if not isinstance(event.payload, expected):
        raise EngineInvariantError(
            f"事件 {event.type} 的 payload 类型不匹配："
            f"期望 {expected.__name__}，实得 {type(event.payload).__name__}"
        )
    updates = _reduce_dispatch(state, event)
    updates["state_version"] = state.state_version + 1
    return state.model_copy(update=updates)
```

(d) `_reduce_dispatch` 末尾的 `# GAME_CREATED / GAME_STARTED 仅审计` 注释与 `return {}` 改为：
```python
    # 前置校验保证 event.type 已映射且 payload 类型正确；至此必已命中上方某分支。
    # 此兜底仅为类型完备性保留（不可达）。
    return {}
```

`backend/app/engine/engine.py`：删除 85-86 行的 `class EngineInvariantError(RuntimeError): ...` 定义（含 docstring），在既有 `from app.engine.events import (...)` import 块中加入 `EngineInvariantError`（保持字母序）。其余 4 处 `raise EngineInvariantError(...)` 调用不动（名字经 import 解析）。

- [ ] **Step 4: 运行确认通过**

Run: `uv run pytest tests/test_fail_loud.py -v`
Expected: PASS（5 项）。

- [ ] **Step 5: 全量回归（核心门：确定性字节级不变）+ 提交**

Run: `uv run pytest -q && uv run mypy app && uv run ruff check . && uv run ruff format --check .`
Expected: 全绿（156 tests）。
```bash
git add backend/app/engine/events.py backend/app/engine/engine.py backend/tests/test_fail_loud.py
git commit -m "feat(engine): reduce 前置校验拒绝畸形/预留事件（EVENT_PAYLOAD_TYPES 映射）"
```

### Task 2: `_apply_sheriff` 显式兜底守卫

**Files:**
- Modify: `backend/app/engine/engine.py`（`_apply_sheriff` 末尾 vote_sheriff 兜底）
- Test: `backend/tests/test_fail_loud.py`（追加）

**Interfaces:**
- Consumes: Task 1 的 `EngineInvariantError`（events 定义、engine re-export）。
- Produces: `_apply_sheriff` 对非 `VOTE_SHERIFF` 的未处理行动类型抛 `EngineInvariantError`（不再静默当投票）。

- [ ] **Step 1: 写测试（正例直测 + 守卫存在性）**

Append 到 `backend/tests/test_fail_loud.py`：
```python
def test_apply_sheriff_vote_positive_path() -> None:
    # 正例：VOTE_SHERIFF 落入兜底并正常发射 SHERIFF_VOTE_CAST
    from app.engine.actions import SheriffAction, SheriffActionType
    from app.engine.engine import _apply_sheriff

    st = _state().model_copy(
        update={"phase": Phase.SHERIFF_ELECTION, "election_stage": "vote", "sheriff_candidates": (1,)}
    )
    new, events = _apply_sheriff(
        st, SheriffAction(actor_seat=2, action_type=SheriffActionType.VOTE_SHERIFF, target_seat=1)
    )
    assert [e.type for e in events] == [EventType.SHERIFF_VOTE_CAST]
    assert new.sheriff_votes == {2: 1}
```
> 说明：抛错分支在当前 6 成员枚举下不可达（全部行动类型被上方分支或此守卫枚举）——作为防御性代码不做单测（无法构造非法枚举成员且不 mock）；由 Task 1 的映射完整性测试提供同类护栏模式。此正例钉死「兜底 == 且仅 == vote_sheriff」。

- [ ] **Step 2: 运行确认现状通过（守卫加入后须保持通过）**

Run: `uv run pytest tests/test_fail_loud.py::test_apply_sheriff_vote_positive_path -v`
Expected: PASS（该正例在改动前后都应通过——本任务的「失败态」体现在代码审查层面而非测试层面，属防御性改动）。

- [ ] **Step 3: 加显式守卫**

`backend/app/engine/engine.py` 的 `_apply_sheriff`，把末尾：
```python
    # vote_sheriff
    s, e = _emit(
```
改为：
```python
    if at != SheriffActionType.VOTE_SHERIFF:
        # 校验层应已拦截；此守卫防止未来枚举增长时静默误分类为投票
        raise EngineInvariantError(f"未处理的警长行动类型：{at}")
    s, e = _emit(
```
（`_emit` 及其后参数行不动。）

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `uv run pytest tests/test_fail_loud.py -v && uv run pytest -q`
Expected: 全绿（157 tests；含确定性/扫描）。
Run: `uv run mypy app && uv run ruff check . && uv run ruff format --check .`
Expected: 干净。

- [ ] **Step 5: 提交**

```bash
git add backend/app/engine/engine.py backend/tests/test_fail_loud.py
git commit -m "feat(engine): _apply_sheriff 兜底显式抛错，闭环 issue #4"
```

---

## Self-Review

**Spec 覆盖**：§2 映射+前置校验→Task 1（含 (a)-(d) 全部四处改动）；§3 异常迁移+re-export→Task 1 Step 3 + `test_exception_reexported_from_engine`；§4 警长兜底→Task 2；§5 测试→两任务测试步骤（不匹配/预留/完整性/正例/re-export/警长正例 + 回归）；§6 范围外无任务——正确。

**占位扫描**：无 TBD/TODO；每步完整代码与期望输出。

**类型一致性**：`EVENT_PAYLOAD_TYPES` 28 项与 events.py 现有 payload 类名逐一对应（`WITCH_SAVED`/`WITCH_POISONED`→`WitchActedPayload` 共用）；`EngineInvariantError` 迁移后 engine.py 4 处 raise 经 import 解析；测试 import 路径（events 直取 + engine re-export）与实现一致。
